import { useState, useEffect } from "react";

const NURSE_1 = "https://images.unsplash.com/photo-1559839734-2b71ea197ec2?w=400&h=500&fit=crop&crop=face";
const NURSE_2 = "https://images.unsplash.com/photo-1651008376811-b90baee60c1f?w=400&h=300&fit=crop&crop=top";
const NURSE_3 = "https://images.unsplash.com/photo-1622253692010-333f2da6031d?w=400&h=350&fit=crop&crop=face";

const Check = ({ green }) => (
  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" style={{flexShrink:0}}>
    <circle cx="10" cy="10" r="9" fill={green ? "#22c55e" : "#2563eb"} />
    <path d="M6 10l3 3 5-5" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
  </svg>
);

const X = () => (
  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" style={{flexShrink:0}}>
    <circle cx="10" cy="10" r="9" fill="#ef4444" />
    <path d="M7 7l6 6M13 7l-6 6" stroke="white" strokeWidth="2" strokeLinecap="round" />
  </svg>
);

export default function NurseBrace() {
  const [timeLeft, setTimeLeft] = useState({ h: 1, m: 47, s: 33 });
  const [showExit, setShowExit] = useState(false);
  const [exitDismissed, setExitDismissed] = useState(false);

  useEffect(() => {
    const t = setInterval(() => {
      setTimeLeft(prev => {
        let { h, m, s } = prev;
        s--;
        if (s < 0) { s = 59; m--; }
        if (m < 0) { m = 59; h--; }
        if (h < 0) { h = 1; m = 59; s = 59; }
        return { h, m, s };
      });
    }, 1000);
    return () => clearInterval(t);
  }, []);

  useEffect(() => {
    if (exitDismissed) return;
    const handle = (e) => { if (e.clientY < 10) setShowExit(true); };
    document.addEventListener("mouseleave", handle);
    return () => document.removeEventListener("mouseleave", handle);
  }, [exitDismissed]);

  const pad = n => String(n).padStart(2, "0");

  return (
    <div style={{ fontFamily: "'Montserrat', sans-serif", margin: 0, padding: 0, color: "#1a1a2e", background: "white", paddingBottom: 70 }}>
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&display=swap');
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Montserrat', sans-serif; }
        .nav-link { color: #333; text-decoration: none; font-size: 14px; font-weight: 600; transition: color 0.2s; }
        .nav-link:hover { color: #2563eb; }
        .btn-nav-outline { background: transparent; color: #2563eb; border: 2px solid #2563eb; padding: 9px 20px; border-radius: 8px; font-size: 13px; font-weight: 700; cursor: pointer; transition: all 0.2s; }
        .btn-nav-outline:hover { background: #eff6ff; }
        .btn-nav { background: #2563eb; color: white; border: none; padding: 9px 20px; border-radius: 8px; font-size: 13px; font-weight: 700; cursor: pointer; transition: all 0.2s; }
        .btn-nav:hover { background: #1d4ed8; transform: translateY(-1px); }
        .hero-btn-wrap { display: inline-block; border-radius: 50px; padding: 5px; border: 2px dashed rgba(255,255,255,0.65); }
        .hero-btn-primary { background: #2563eb; color: white; border: 2px solid rgba(255,255,255,0.3); padding: 13px 24px; border-radius: 50px; font-size: 14px; font-weight: 800; cursor: pointer; transition: all 0.2s; display: inline-flex; align-items: center; gap: 8px; }
        .hero-btn-primary:hover { background: #1d4ed8; transform: translateY(-2px); box-shadow: 0 8px 24px rgba(37,99,235,0.45); }
        .hero-btn-outline { background: white; color: #1d4ed8; border: 2px solid rgba(255,255,255,0.3); padding: 13px 24px; border-radius: 50px; font-size: 14px; font-weight: 800; cursor: pointer; transition: all 0.2s; }
        .hero-btn-outline:hover { background: #f0f4ff; transform: translateY(-2px); }
        .big-cta { background: #2563eb; color: white; border: none; padding: 17px 36px; border-radius: 12px; font-size: 16px; font-weight: 800; cursor: pointer; transition: all 0.2s; display: inline-block; }
        .big-cta:hover { background: #1d4ed8; transform: translateY(-3px); box-shadow: 0 12px 32px rgba(37,99,235,0.4); }
        .big-cta-green { background: #16a34a; color: white; border: none; padding: 17px 36px; border-radius: 12px; font-size: 16px; font-weight: 800; cursor: pointer; transition: all 0.2s; display: inline-block; }
        .big-cta-green:hover { background: #15803d; transform: translateY(-3px); box-shadow: 0 12px 32px rgba(22,163,74,0.4); }
        .pain-item { display: flex; align-items: flex-start; gap: 14px; padding: 18px 20px; background: white; border-radius: 12px; border-left: 4px solid #ef4444; box-shadow: 0 2px 12px rgba(0,0,0,0.06); margin-bottom: 14px; }
        .solution-item { display: flex; align-items: flex-start; gap: 14px; padding: 16px 20px; background: #f0fdf4; border-radius: 12px; border-left: 4px solid #22c55e; margin-bottom: 12px; }
        .what-get-item { display: flex; align-items: center; gap: 14px; padding: 14px 0; border-bottom: 1px solid #f1f5f9; font-size: 15px; font-weight: 600; color: #1e293b; }
        .diff-bad { display: flex; align-items: flex-start; gap: 12px; padding: 14px 18px; background: #fef2f2; border-radius: 10px; margin-bottom: 10px; font-size: 14px; color: #7f1d1d; font-weight: 600; }
        .diff-good { display: flex; align-items: flex-start; gap: 12px; padding: 14px 18px; background: #f0fdf4; border-radius: 10px; margin-bottom: 10px; font-size: 14px; color: #14532d; font-weight: 600; }
        .testimonial-card { background: white; border-radius: 16px; padding: 28px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); border: 1px solid #e2e8f0; flex: 1; min-width: 260px; transition: transform 0.2s; }
        .testimonial-card:hover { transform: translateY(-4px); }
        .pricing-card { background: white; border-radius: 20px; padding: 36px 28px; box-shadow: 0 2px 16px rgba(0,0,0,0.08); border: 2px solid #e5e7eb; flex: 1; min-width: 240px; max-width: 320px; transition: transform 0.2s; }
        .pricing-card:hover { transform: translateY(-4px); }
        .pricing-card.popular { border-color: #2563eb; box-shadow: 0 8px 40px rgba(37,99,235,0.18); }
        .subscribe-btn { width: 100%; background: #2563eb; color: white; border: none; padding: 14px; border-radius: 10px; font-size: 14px; font-weight: 800; cursor: pointer; margin-top: 20px; transition: background 0.2s; }
        .subscribe-btn:hover { background: #1d4ed8; }
        .subscribe-btn-outline { width: 100%; background: transparent; color: #2563eb; border: 2px solid #2563eb; padding: 14px; border-radius: 10px; font-size: 14px; font-weight: 800; cursor: pointer; margin-top: 20px; transition: all 0.2s; }
        .subscribe-btn-outline:hover { background: #eff6ff; }
        .check-item { display: flex; align-items: center; gap: 10px; margin-bottom: 12px; font-size: 14px; color: #374151; font-weight: 500; }
        .footer-link { color: #2563eb; text-decoration: none; font-size: 13px; display: block; margin-bottom: 10px; font-weight: 500; transition: color 0.2s; }
        .footer-link:hover { color: #1d4ed8; text-decoration: underline; }
        .sticky-bar { position: fixed; bottom: 0; left: 0; right: 0; background: #1d4ed8; color: white; display: flex; align-items: center; justify-content: center; gap: 20px; padding: 13px 24px; z-index: 999; box-shadow: 0 -4px 20px rgba(0,0,0,0.2); }
        .exit-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.6); z-index: 9999; display: flex; align-items: center; justify-content: center; }
        .exit-modal { background: white; border-radius: 20px; padding: 48px 40px; max-width: 480px; width: 90%; text-align: center; }
        @media (max-width: 768px) {
          .hero-grid { flex-direction: column !important; }
          .two-col { flex-direction: column !important; }
          .pricing-grid { flex-direction: column !important; align-items: center !important; }
          .footer-grid { flex-direction: column !important; gap: 32px !important; }
          .sticky-bar { flex-direction: column; gap: 8px; padding: 12px 16px; }
          .testimonials-grid { flex-direction: column !important; }
        }
      `}</style>

      {/* NAVBAR */}
      <nav style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "0 40px", height: "64px", background: "white", borderBottom: "3px solid #2563eb", position: "sticky", top: 0, zIndex: 100, boxShadow: "0 2px 12px rgba(0,0,0,0.06)" }}>
        <span style={{ fontWeight: 900, fontSize: 22, color: "#2563eb", letterSpacing: "-0.5px" }}>NurseBrace</span>
        <div style={{ display: "flex", gap: 32, alignItems: "center" }}>
          <a href="#" className="nav-link">Exams</a>
          <a href="#" className="nav-link">Pricing</a>
          <a href="#" className="nav-link">About</a>
        </div>
        <div style={{ display: "flex", gap: 10 }}>
          <button className="btn-nav-outline">Sign In</button>
          <button className="btn-nav">Get Started</button>
        </div>
      </nav>

      {/* HERO */}
      <section style={{ background: "linear-gradient(135deg, #1d4ed8 0%, #2563eb 60%, #3b82f6 100%)", padding: "64px 40px", minHeight: "480px", display: "flex", alignItems: "center" }}>
        <div style={{ maxWidth: 1200, margin: "0 auto", width: "100%", display: "flex", alignItems: "center", gap: 60 }} className="hero-grid">
          <div style={{ flex: "0 0 52%", color: "white" }}>
            <div style={{ display: "inline-block", background: "rgba(255,255,255,0.18)", border: "1px solid rgba(255,255,255,0.35)", borderRadius: 20, padding: "6px 16px", fontSize: 12, fontWeight: 700, letterSpacing: 1, marginBottom: 20, textTransform: "uppercase" }}>
              🔥 For Students Testing in the Next 2–4 Weeks
            </div>
            <h1 style={{ fontWeight: 900, fontSize: "clamp(26px,3.5vw,46px)", lineHeight: 1.14, marginBottom: 18 }}>
              Pass Your TEAS/HESI Exam on Your First Try —{" "}
              <span style={{ color: "#fbbf24" }}>Without Wasting Time on the Wrong Materials</span>
            </h1>
            <p style={{ fontSize: 15, lineHeight: 1.75, opacity: 0.93, marginBottom: 32, maxWidth: 480, fontWeight: 500 }}>
              Get access to high-yield, exam-level practice questions with detailed rationales — designed for students testing in the next 2–4 weeks.
            </p>
            <div style={{ display: "flex", gap: 16, flexWrap: "wrap", marginBottom: 28, alignItems: "center" }}>
              <div className="hero-btn-wrap">
                <button className="hero-btn-primary">👉 Start Practicing Now (Instant Access)</button>
              </div>
              <div className="hero-btn-wrap">
                <button className="hero-btn-outline">👉 Try a Free Sample Test</button>
              </div>
            </div>
            <div style={{ display: "flex", flexDirection: "column", gap: 7 }}>
              {["No credit card required", "Cancel anytime", "30-question free trial"].map(t => (
                <div key={t} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13, color: "rgba(255,255,255,0.9)", fontWeight: 600 }}>
                  <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8l3.5 3.5L13 5" stroke="white" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round" /></svg>
                  {t}
                </div>
              ))}
            </div>
          </div>
          <div style={{ flex: 1, display: "grid", gridTemplateColumns: "1fr 1fr", gridTemplateRows: "auto auto", gap: 14 }}>
            <div style={{ gridRow: "1 / 3", borderRadius: 16, overflow: "hidden", boxShadow: "0 8px 32px rgba(0,0,0,0.28)" }}>
              <img src={NURSE_1} alt="Nurse" style={{ width: "100%", height: "100%", objectFit: "cover", display: "block" }} />
            </div>
            <div style={{ borderRadius: 16, overflow: "hidden", boxShadow: "0 8px 32px rgba(0,0,0,0.22)" }}>
              <img src={NURSE_2} alt="Nurse studying" style={{ width: "100%", height: "100%", objectFit: "cover", display: "block" }} />
            </div>
            <div style={{ borderRadius: 16, overflow: "hidden", boxShadow: "0 8px 32px rgba(0,0,0,0.22)" }}>
              <img src={NURSE_3} alt="Nurse" style={{ width: "100%", height: "100%", objectFit: "cover", display: "block" }} />
            </div>
          </div>
        </div>
      </section>

      {/* PAIN POINT */}
      <section style={{ background: "#fff7f7", padding: "80px 40px" }}>
        <div style={{ maxWidth: 780, margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: 48 }}>
            <span style={{ fontSize: 36 }}>😬</span>
            <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12 }}>Tired of Studying and Still Not Passing?</h2>
          </div>
          {[
            "You've been reading, watching videos… but questions still feel unfamiliar on exam day.",
            "You're running out of time before your test — and the pressure is mounting.",
            "You may have failed before — and you absolutely cannot go through that again.",
          ].map((text, i) => (
            <div key={i} className="pain-item">
              <span style={{ fontSize: 22, marginTop: 2 }}>😔</span>
              <p style={{ fontSize: 15, color: "#374151", fontWeight: 600, lineHeight: 1.65 }}>{text}</p>
            </div>
          ))}
          <div style={{ background: "#1e293b", color: "white", borderRadius: 14, padding: "24px 28px", marginTop: 28, textAlign: "center" }}>
            <p style={{ fontSize: 17, fontWeight: 800, lineHeight: 1.65 }}>
              💡 The truth? <span style={{ color: "#fbbf24" }}>Most students fail because they study the wrong way.</span><br />
              <span style={{ fontWeight: 500, fontSize: 14, opacity: 0.8 }}>Not because they're not smart enough.</span>
            </p>
          </div>
        </div>
      </section>

      {/* SOLUTION */}
      <section style={{ background: "#f0fdf4", padding: "80px 40px" }}>
        <div style={{ maxWidth: 780, margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: 48 }}>
            <span style={{ fontSize: 36 }}>💡</span>
            <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12 }}>This Is What Actually Works</h2>
            <p style={{ fontSize: 15, color: "#6b7280", marginTop: 12, fontWeight: 500 }}>Our practice tests are built to:</p>
          </div>
          {[
            "Focus on high-yield topics that repeatedly appear on real exams",
            "Match the exact difficulty and style of real TEAS/HESI questions",
            "Train you to think the way the exam wants you to think",
          ].map((item, i) => (
            <div key={i} className="solution-item">
              <Check green />
              <p style={{ fontSize: 15, color: "#15803d", fontWeight: 700, lineHeight: 1.6 }}>{item}</p>
            </div>
          ))}
          <div style={{ background: "#2563eb", color: "white", borderRadius: 14, padding: "22px 28px", marginTop: 28, textAlign: "center" }}>
            <p style={{ fontSize: 17, fontWeight: 800 }}>💥 So when you sit for your test… <span style={{ color: "#fbbf24" }}>nothing feels new.</span></p>
          </div>
        </div>
      </section>

      {/* WHAT YOU GET */}
      <section style={{ background: "white", padding: "80px 40px" }}>
        <div style={{ maxWidth: 780, margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: 48 }}>
            <span style={{ fontSize: 36 }}>📊</span>
            <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12 }}>What You'll Get Inside</h2>
          </div>
          <div style={{ background: "#f8fafc", borderRadius: 20, padding: "32px 36px", border: "2px solid #e2e8f0" }}>
            {[
              "1,000+ exam-level practice questions",
              "Detailed rationales — not just answers, but WHY",
              "Coverage of ALL key sections: Reading, Math, Science, A&P",
              "Real exam patterns and commonly tested topics",
              "Instant access — start practicing in minutes",
            ].map((item, i) => (
              <div key={i} className="what-get-item"><Check green /><span>{item}</span></div>
            ))}
          </div>
          <div style={{ textAlign: "center", marginTop: 36 }}>
            <button className="big-cta">👉 Start Practicing Now — Instant Access</button>
          </div>
        </div>
      </section>

      {/* DIFFERENTIATION */}
      <section style={{ background: "#f8fafc", padding: "80px 40px" }}>
        <div style={{ maxWidth: 900, margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: 48 }}>
            <span style={{ fontSize: 36 }}>🧠</span>
            <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12 }}>Why This Works Better Than Free Practice Tests</h2>
          </div>
          <div style={{ display: "flex", gap: 32, flexWrap: "wrap" }} className="two-col">
            <div style={{ flex: 1, minWidth: 260 }}>
              <p style={{ fontWeight: 800, fontSize: 14, color: "#ef4444", marginBottom: 14, textTransform: "uppercase", letterSpacing: 0.5 }}>❌ Free Tests</p>
              {["Too easy or completely outdated", "Don't reflect real exam difficulty", "No proper explanations — just answer keys", "Waste your precious study time"].map((t, i) => (
                <div key={i} className="diff-bad"><X /><span>{t}</span></div>
              ))}
            </div>
            <div style={{ flex: 1, minWidth: 260 }}>
              <p style={{ fontWeight: 800, fontSize: 14, color: "#22c55e", marginBottom: 14, textTransform: "uppercase", letterSpacing: 0.5 }}>✅ NurseBrace</p>
              {["Focuses only on what's actually tested", "Questions match real exam difficulty", "Deep rationales so you understand — not memorize", "Built for students testing THIS month"].map((t, i) => (
                <div key={i} className="diff-good"><Check green /><span>{t}</span></div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* SOCIAL PROOF */}
      <section style={{ background: "white", padding: "80px 40px" }}>
        <div style={{ maxWidth: 1100, margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: 52 }}>
            <span style={{ fontSize: 36 }}>🏆</span>
            <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12 }}>Real Students. Real Results.</h2>
            <div style={{ display: "flex", justifyContent: "center", gap: 3, marginTop: 10, alignItems: "center" }}>
              {"★★★★★".split("").map((s, i) => <span key={i} style={{ color: "#f59e0b", fontSize: 22 }}>{s}</span>)}
              <span style={{ color: "#6b7280", fontSize: 14, fontWeight: 600, marginLeft: 8 }}>4.9/5 — 10,000+ Students</span>
            </div>
          </div>
          <div style={{ display: "flex", gap: 24, flexWrap: "wrap" }} className="testimonials-grid">
            {[
              { quote: "I failed my first attempt… used NurseBrace and passed on my second try! The questions were so much like the real thing.", name: "Maria T.", tag: "TEAS — Second attempt ✅" },
              { quote: "The questions were VERY similar to my actual exam. I felt prepared in a way I never had before. Worth every penny.", name: "James K.", tag: "HESI A2 — First attempt ✅" },
              { quote: "The rationales helped me understand topics I always struggled with. This is NOT like the free stuff you find online.", name: "Aisha R.", tag: "ATI TEAS — First attempt ✅" },
            ].map((t, i) => (
              <div key={i} className="testimonial-card">
                <div style={{ display: "flex", gap: 2, marginBottom: 14 }}>
                  {"★★★★★".split("").map((s, j) => <span key={j} style={{ color: "#f59e0b", fontSize: 18 }}>{s}</span>)}
                </div>
                <p style={{ fontSize: 14, color: "#374151", lineHeight: 1.75, fontStyle: "italic", fontWeight: 500, marginBottom: 18 }}>"{t.quote}"</p>
                <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
                  <div style={{ width: 40, height: 40, borderRadius: "50%", background: "#2563eb", display: "flex", alignItems: "center", justifyContent: "center", color: "white", fontWeight: 800, fontSize: 15, flexShrink: 0 }}>{t.name[0]}</div>
                  <div>
                    <p style={{ fontWeight: 800, fontSize: 13, color: "#111827" }}>{t.name}</p>
                    <p style={{ fontSize: 12, color: "#22c55e", fontWeight: 700 }}>{t.tag}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
          <div style={{ textAlign: "center", marginTop: 36, padding: "18px 24px", background: "#fffbeb", borderRadius: 12, border: "2px dashed #f59e0b" }}>
            <p style={{ fontSize: 14, color: "#92400e", fontWeight: 700 }}>📸 Insert real screenshots / testimonials here — this is the #1 conversion driver on your page</p>
          </div>
        </div>
      </section>

      {/* STATS */}
      <section style={{ background: "#1e293b", padding: "60px 40px" }}>
        <div style={{ maxWidth: 1000, margin: "0 auto", display: "flex", gap: 20, flexWrap: "wrap", justifyContent: "center" }}>
          {[
            { value: "95%", label: "Pass Rate", sub: "Students improved their scores" },
            { value: "10,000+", label: "Students Helped", sub: "Across the country" },
            { value: "4.9/5", label: "Student Rating", sub: "Based on verified reviews" },
            { value: "1,000+", label: "Practice Questions", sub: "High-yield, exam-level" },
          ].map((s, i) => (
            <div key={i} style={{ textAlign: "center", flex: 1, minWidth: 180, padding: "24px 20px", background: "rgba(255,255,255,0.06)", borderRadius: 16, border: "1px solid rgba(255,255,255,0.1)" }}>
              <p style={{ fontWeight: 900, fontSize: 34, color: "#fbbf24", marginBottom: 4 }}>{s.value}</p>
              <p style={{ fontWeight: 800, fontSize: 14, color: "white", marginBottom: 4 }}>{s.label}</p>
              <p style={{ fontSize: 12, color: "rgba(255,255,255,0.55)", fontWeight: 500 }}>{s.sub}</p>
            </div>
          ))}
        </div>
      </section>

      {/* URGENCY */}
      <section style={{ background: "#fff7ed", padding: "72px 40px", textAlign: "center" }}>
        <div style={{ maxWidth: 680, margin: "0 auto" }}>
          <span style={{ fontSize: 40 }}>⏳</span>
          <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12, marginBottom: 14 }}>Taking Your Exam Soon?</h2>
          <p style={{ fontSize: 16, color: "#374151", fontWeight: 600, lineHeight: 1.7, marginBottom: 28 }}>
            NurseBrace is designed specifically for students testing in the{" "}
            <span style={{ color: "#ea580c", fontWeight: 800 }}>next 2–4 weeks.</span><br />
            Don't waste time on random materials — focus on what actually matters.
          </p>
          <div style={{ background: "#ea580c", color: "white", borderRadius: 14, padding: "20px 28px", marginBottom: 28, display: "inline-block" }}>
            <p style={{ fontWeight: 700, fontSize: 13, marginBottom: 10, opacity: 0.9 }}>⚠️ LIMITED OFFER — ENDS IN:</p>
            <div style={{ display: "flex", gap: 14, justifyContent: "center", alignItems: "center" }}>
              {[["h", timeLeft.h], ["m", timeLeft.m], ["s", timeLeft.s]].map(([label, val]) => (
                <div key={label} style={{ textAlign: "center" }}>
                  <div style={{ background: "rgba(0,0,0,0.25)", borderRadius: 8, padding: "8px 16px", fontWeight: 900, fontSize: 28, minWidth: 56 }}>{pad(val)}</div>
                  <div style={{ fontSize: 11, marginTop: 4, opacity: 0.85, fontWeight: 700, textTransform: "uppercase" }}>{label}</div>
                </div>
              ))}
            </div>
          </div>
          <br />
          <p style={{ fontSize: 13, color: "#9ca3af", fontWeight: 600, marginBottom: 24 }}>🔒 Only for students testing this month</p>
          <button className="big-cta">👉 Start Practicing Now — Don't Wait</button>
        </div>
      </section>

      {/* PRICING */}
      <section style={{ background: "#f8fafc", padding: "80px 40px" }}>
        <div style={{ maxWidth: 1100, margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: 52 }}>
            <span style={{ fontSize: 36 }}>💰</span>
            <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 12 }}>Get Instant Access Today</h2>
            <p style={{ fontSize: 15, color: "#6b7280", marginTop: 10, fontWeight: 500 }}>One subscription gives you access to all exams. No hidden fees, no surprises.</p>
          </div>
          <div style={{ display: "flex", gap: 24, flexWrap: "wrap", justifyContent: "center" }} className="pricing-grid">
            <div className="pricing-card">
              <h3 style={{ fontWeight: 800, fontSize: 22, color: "#111827", marginBottom: 4 }}>Free Trial</h3>
              <p style={{ fontWeight: 900, fontSize: 32, color: "#2563eb", marginBottom: 6 }}>$0</p>
              <p style={{ fontSize: 13, color: "#9ca3af", marginBottom: 22, fontWeight: 500 }}>No credit card required</p>
              {["30 practice questions", "All question categories", "Detailed explanations"].map(f => (
                <div key={f} className="check-item"><Check />{f}</div>
              ))}
              <button className="subscribe-btn-outline">👉 Try a Free Sample Test</button>
            </div>
            <div className="pricing-card popular" style={{ position: "relative" }}>
              <div style={{ position: "absolute", top: -14, left: 24, background: "#2563eb", color: "white", borderRadius: 20, padding: "5px 16px", fontSize: 12, fontWeight: 800 }}>🔥 Most Popular</div>
              <h3 style={{ fontWeight: 800, fontSize: 22, color: "#111827", marginBottom: 4 }}>Monthly Plan</h3>
              <p style={{ fontWeight: 900, fontSize: 32, color: "#2563eb", marginBottom: 2 }}>$49.99<span style={{ fontSize: 15, fontWeight: 600 }}>/mo</span></p>
              <p style={{ fontSize: 13, color: "#9ca3af", marginBottom: 22, fontWeight: 500 }}>Full access — cancel anytime</p>
              {["Unlimited practice sessions", "50 questions per session", "All features included", "Regular content updates"].map(f => (
                <div key={f} className="check-item"><Check />{f}</div>
              ))}
              <button className="subscribe-btn">👉 Get Instant Access Now</button>
            </div>
            <div className="pricing-card">
              <h3 style={{ fontWeight: 800, fontSize: 22, color: "#111827", marginBottom: 4 }}>Weekly Plan</h3>
              <p style={{ fontWeight: 900, fontSize: 32, color: "#2563eb", marginBottom: 6 }}>$19.99<span style={{ fontSize: 15, fontWeight: 600 }}>/wk</span></p>
              <p style={{ fontSize: 13, color: "#9ca3af", marginBottom: 22, fontWeight: 500 }}>Perfect for last-minute prep</p>
              {["Unlimited practice sessions", "50 questions per session", "All features included"].map(f => (
                <div key={f} className="check-item"><Check />{f}</div>
              ))}
              <button className="subscribe-btn-outline">👉 Subscribe</button>
            </div>
          </div>
        </div>
      </section>

      {/* RISK REVERSAL */}
      <section style={{ background: "white", padding: "72px 40px", textAlign: "center" }}>
        <div style={{ maxWidth: 600, margin: "0 auto" }}>
          <span style={{ fontSize: 48 }}>🛡️</span>
          <h2 style={{ fontWeight: 900, fontSize: "clamp(24px,3vw,36px)", color: "#111827", marginTop: 16, marginBottom: 16 }}>Try It Risk-Free</h2>
          <p style={{ fontSize: 16, color: "#374151", lineHeight: 1.75, fontWeight: 500, marginBottom: 28 }}>
            If you don't find NurseBrace helpful, you're fully covered by our <strong>money-back guarantee.</strong><br />No questions asked. No hoops to jump through.
          </p>
          <div style={{ background: "#f0fdf4", border: "2px solid #22c55e", borderRadius: 14, padding: "20px 28px", marginBottom: 32 }}>
            <p style={{ fontWeight: 800, fontSize: 16, color: "#15803d" }}>✅ No risk. Just results.</p>
          </div>
          <button className="big-cta-green">👉 Get Instant Access — Risk Free</button>
        </div>
      </section>

      {/* FREE SAMPLE BONUS */}
      <section style={{ background: "#fffbeb", padding: "60px 40px", textAlign: "center", borderTop: "3px dashed #f59e0b" }}>
        <div style={{ maxWidth: 600, margin: "0 auto" }}>
          <span style={{ fontSize: 40 }}>🎁</span>
          <h2 style={{ fontWeight: 900, fontSize: "clamp(22px,2.5vw,32px)", color: "#111827", marginTop: 12, marginBottom: 12 }}>FREE Sample Test Available</h2>
          <p style={{ fontSize: 15, color: "#374151", fontWeight: 500, lineHeight: 1.7, marginBottom: 24 }}>
            Not sure yet? Try before you buy — see the quality yourself. No credit card, no commitment.
          </p>
          <button className="big-cta" style={{ background: "#d97706" }}>👉 Try a Free Sample Test Now</button>
        </div>
      </section>

      {/* FINAL CTA */}
      <section style={{ background: "linear-gradient(135deg, #1d4ed8, #2563eb)", padding: "80px 40px", textAlign: "center", color: "white" }}>
        <div style={{ maxWidth: 660, margin: "0 auto" }}>
          <span style={{ fontSize: 40 }}>🎯</span>
          <h2 style={{ fontWeight: 900, fontSize: "clamp(28px,3.5vw,44px)", marginTop: 16, marginBottom: 16, lineHeight: 1.2 }}>
            Your Exam Is Coming.<br /><span style={{ color: "#fbbf24" }}>Be Ready.</span>
          </h2>
          <p style={{ fontSize: 16, opacity: 0.92, lineHeight: 1.75, fontWeight: 500, marginBottom: 36 }}>
            Every day you wait is a missed opportunity to improve your score.<br />
            Start practicing now and walk into your exam with full confidence.
          </p>
          <div style={{ display: "flex", gap: 16, justifyContent: "center", flexWrap: "wrap" }}>
            <button className="big-cta" style={{ background: "white", color: "#1d4ed8", fontSize: 16 }}>👉 Get Instant Access</button>
            <button className="big-cta" style={{ background: "rgba(255,255,255,0.12)", border: "2px solid rgba(255,255,255,0.5)", fontSize: 16 }}>👉 Try Free Sample</button>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer style={{ background: "#0f172a", padding: "56px 40px 32px" }}>
        <div style={{ maxWidth: 1200, margin: "0 auto" }}>
          <div style={{ display: "flex", gap: 60, flexWrap: "wrap", marginBottom: 48 }} className="footer-grid">
            <div style={{ flex: "0 0 200px" }}>
              <span style={{ fontWeight: 900, fontSize: 22, color: "#2563eb" }}>NurseBrace</span>
              <p style={{ fontSize: 13, color: "#94a3b8", marginTop: 10, lineHeight: 1.65, fontWeight: 500 }}>Empowering nursing students to pass on the first try.</p>
            </div>
            {[
              { label: "Product", links: ["Exams", "Pricing"] },
              { label: "Company", links: ["About Us", "Contact"] },
              { label: "Resources", links: ["FAQ"] },
              { label: "Legal", links: ["Privacy Policy", "Terms of Service"] },
            ].map(col => (
              <div key={col.label}>
                <p style={{ fontWeight: 800, fontSize: 12, color: "#e2e8f0", marginBottom: 16, borderBottom: "1px solid rgba(255,255,255,0.1)", paddingBottom: 8, textTransform: "uppercase", letterSpacing: 0.8 }}>{col.label}</p>
                {col.links.map(l => <a key={l} href="#" className="footer-link">{l}</a>)}
              </div>
            ))}
          </div>
          <div style={{ borderTop: "1px solid rgba(255,255,255,0.08)", paddingTop: 24, display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 16 }}>
            <p style={{ fontSize: 13, color: "#64748b", fontWeight: 500 }}>© 2026 NurseBrace. All rights reserved.</p>
          </div>
        </div>
      </footer>

      {/* STICKY BAR */}
      <div className="sticky-bar">
        <p style={{ fontWeight: 600, fontSize: 14 }}>🔥 <strong>Only for students testing this month</strong> — Don't leave without starting your free trial</p>
        <button className="big-cta" style={{ padding: "11px 28px", fontSize: 14, whiteSpace: "nowrap", background: "#fbbf24", color: "#1e293b" }}>
          👉 Start Now — Free
        </button>
      </div>

      {/* EXIT POPUP */}
      {showExit && !exitDismissed && (
        <div className="exit-overlay" onClick={() => { setShowExit(false); setExitDismissed(true); }}>
          <div className="exit-modal" onClick={e => e.stopPropagation()}>
            <div style={{ fontSize: 48, marginBottom: 12 }}>⏸️</div>
            <h3 style={{ fontWeight: 900, fontSize: 24, color: "#111827", marginBottom: 12 }}>Wait! Don't Leave Empty-Handed</h3>
            <p style={{ fontSize: 15, color: "#6b7280", lineHeight: 1.7, marginBottom: 24, fontWeight: 500 }}>
              Get a <strong>FREE sample test</strong> before you go — see the quality yourself with zero commitment.
            </p>
            <button className="big-cta" style={{ width: "100%", marginBottom: 12 }}>🎁 Yes, Give Me the Free Test</button>
            <button onClick={() => { setShowExit(false); setExitDismissed(true); }} style={{ background: "none", border: "none", color: "#9ca3af", fontSize: 13, cursor: "pointer", fontWeight: 600 }}>
              No thanks, I don't want to prepare
            </button>
          </div>
        </div>
      )}
    </div>
  );
}
