import React, { useState, useMemo } from 'react';
import { 
  LayoutDashboard, 
  Users, 
  LineChart, 
  FileText, 
  Megaphone, 
  Settings, 
  Search, 
  TrendingUp, 
  TrendingDown, 
  MoreVertical, 
  Plus, 
  Mail, 
  AlertCircle,
  ChevronRight,
  UserPlus, 
  ShieldCheck,
  Globe,
  Clock
} from 'lucide-react';

// --- Mock Data ---
const MOCK_USERS = [
  { id: 1, name: 'Alex Thompson', email: 'alex@example.com', plan: 'Pro', status: 'Active', winRate: '68%', lastLogin: '2h ago' },
  { id: 2, name: 'Sarah Chen', email: 'sarah.c@trading.io', plan: 'Free', status: 'Inactive', winRate: '42%', lastLogin: '3d ago' },
  { id: 3, name: 'Marcus Miller', email: 'marcus@fx.net', plan: 'Enterprise', status: 'Active', winRate: '71%', lastLogin: '15m ago' },
  { id: 4, name: 'Elena Rodriguez', email: 'elena.r@crypto.com', plan: 'Pro', status: 'Banned', winRate: '55%', lastLogin: '1w ago' },
];

const MOCK_POSTS = [
  { id: 1, title: 'Weekly Market Recap: CPI Volatility', author: 'Admin', date: '2023-10-24', status: 'Published' },
  { id: 2, title: 'Top 5 Psychological Biases in Trading', author: 'Admin', date: '2023-10-22', status: 'Draft' },
];

const GROWTH_DATA = [40, 55, 45, 70, 65, 85, 75, 90, 80, 95, 88, 110];

// --- Sub-Components ---

const StatCard = ({ title, value, change, trend, icon: Icon }) => (
  <div className="bg-slate-900 border border-slate-800 p-6 rounded-xl shadow-sm">
    <div className="flex justify-between items-start mb-4">
      <div className="p-2 bg-indigo-500/10 rounded-lg text-indigo-400">
        <Icon size={20} />
      </div>
      <span className={`text-xs font-semibold px-2 py-1 rounded-full ${trend === 'up' ? 'bg-emerald-500/10 text-emerald-400' : 'bg-rose-500/10 text-rose-400'}`}>
        {change}
      </span>
    </div>
    <h3 className="text-slate-400 text-[10px] font-semibold uppercase tracking-widest truncate" title={title}>{title}</h3>
    <p className="text-2xl font-bold text-white mt-1 tracking-tight">{value}</p>
  </div>
);

const SidebarItem = ({ id, active, label, icon: Icon, onClick }) => (
  <button
    onClick={() => onClick(id)}
    className={`w-full flex items-center gap-3 px-4 py-3 rounded-lg transition-all duration-200 ${
      active ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-400 hover:bg-slate-800 hover:text-slate-200'
    }`}
  >
    <Icon size={20} />
    <span className="font-semibold text-sm">{label}</span>
  </button>
);

const LineGraph = ({ data }) => {
  const width = 500;
  const height = 200;
  const padding = 20;
  
  const maxValue = Math.max(...data);
  const points = data.map((val, i) => {
    const x = (i / (data.length - 1)) * (width - padding * 2) + padding;
    const y = height - ((val / maxValue) * (height - padding * 2) + padding);
    return `${x},${y}`;
  }).join(' ');

  const areaPoints = `20,${height} ${points} ${width - padding},${height}`;

  return (
    <div className="w-full overflow-hidden">
      <svg viewBox={`0 0 ${width} ${height}`} className="w-full h-64 drop-shadow-2xl">
        <defs>
          <linearGradient id="lineGradient" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stopColor="#6366f1" stopOpacity="0.3" />
            <stop offset="100%" stopColor="#6366f1" stopOpacity="0" />
          </linearGradient>
        </defs>
        
        {/* Grid Lines */}
        {[0, 0.25, 0.5, 0.75, 1].map((p, i) => (
          <line 
            key={i}
            x1={padding} 
            y1={padding + (height - padding * 2) * p} 
            x2={width - padding} 
            y2={padding + (height - padding * 2) * p} 
            stroke="#1e293b" 
            strokeWidth="1"
          />
        ))}

        {/* Fill Area */}
        <polyline
          fill="url(#lineGradient)"
          points={areaPoints}
        />

        {/* The Line */}
        <polyline
          fill="none"
          stroke="#6366f1"
          strokeWidth="3"
          strokeLinecap="round"
          strokeLinejoin="round"
          points={points}
        />

        {/* Data Points */}
        {data.map((val, i) => {
          const x = (i / (data.length - 1)) * (width - padding * 2) + padding;
          const y = height - ((val / maxValue) * (height - padding * 2) + padding);
          return (
            <circle 
              key={i} 
              cx={x} 
              cy={y} 
              r="4" 
              className="fill-indigo-500 stroke-slate-900 stroke-2 hover:r-6 transition-all cursor-pointer"
            />
          );
        })}
      </svg>
    </div>
  );
};

// --- Main App Component ---

export default function App() {
  const [activeTab, setActiveTab] = useState('dashboard');
  const [searchQuery, setSearchQuery] = useState('');

  // Sections
  const renderContent = () => {
    switch (activeTab) {
      case 'dashboard':
        return (
          <div className="space-y-6">
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
              <StatCard title="Total Traders" value="12,842" change="+12.5%" trend="up" icon={Users} />
              <StatCard title="Monthly Visitors" value="42.5k" change="+8.2%" trend="up" icon={TrendingUp} />
              <StatCard title="Avg. Session Dur." value="12m 42s" change="+4.1%" trend="up" icon={Clock} />
              <StatCard title="Subscription MRR" value="$84,200" change="+18.3%" trend="up" icon={Globe} />
            </div>

            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
              <div className="lg:col-span-2 bg-slate-900 border border-slate-800 p-6 rounded-xl">
                <div className="flex justify-between items-center mb-6">
                  <h3 className="text-lg font-bold text-white flex items-center gap-2">
                    <TrendingUp size={20} className="text-emerald-400" /> Growth Analytics
                  </h3>
                  <div className="flex items-center gap-3">
                    <div className="flex items-center gap-1.5 px-3 py-1 bg-indigo-500/10 rounded-full border border-indigo-500/20">
                      <div className="w-1.5 h-1.5 rounded-full bg-indigo-500" />
                      <span className="text-[10px] font-bold text-indigo-400 uppercase tracking-tighter">New Traders</span>
                    </div>
                    <select className="bg-slate-800 text-slate-300 text-xs font-medium rounded-md px-3 py-1.5 border border-slate-700 outline-none cursor-pointer">
                      <option>Last 12 Months</option>
                      <option>Last 30 Days</option>
                    </select>
                  </div>
                </div>
                <div className="w-full">
                  <LineGraph data={GROWTH_DATA} />
                </div>
              </div>
              
              <div className="bg-slate-900 border border-slate-800 p-6 rounded-xl">
                <h3 className="text-lg font-bold text-white mb-4">Recent Activity</h3>
                <div className="space-y-4">
                  {[
                    { text: 'New Enterprise signup: Marcus', time: '2m ago', type: 'user' },
                    { text: 'Critical Error: Binance API lag', time: '14m ago', type: 'error' },
                    { text: 'Blog post published: CPI Recap', time: '1h ago', type: 'content' },
                  ].map((act, i) => (
                    <div key={i} className="flex gap-3 text-sm">
                      <div className={`w-2 h-2 mt-1.5 rounded-full ${act.type === 'error' ? 'bg-rose-500' : 'bg-indigo-500'}`} />
                      <div>
                        <p className="text-slate-300 font-medium tracking-tight">{act.text}</p>
                        <p className="text-slate-500 text-xs mt-0.5">{act.time}</p>
                      </div>
                    </div>
                  ))}
                </div>
                <button className="w-full mt-6 py-2.5 text-indigo-400 text-xs font-bold uppercase tracking-widest hover:text-indigo-300 transition-colors border border-indigo-500/20 rounded-lg bg-indigo-500/5">
                  View All Logs
                </button>
              </div>
            </div>
          </div>
        );

      case 'users':
        return (
          <div className="bg-slate-900 border border-slate-800 rounded-xl overflow-hidden">
            <div className="p-6 border-b border-slate-800 flex flex-col md:flex-row justify-between gap-4">
              <div className="relative">
                <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" size={18} />
                <input 
                  type="text" 
                  placeholder="Search traders..." 
                  className="bg-slate-800 text-white pl-10 pr-4 py-2 rounded-lg border border-slate-700 focus:outline-none focus:border-indigo-500 w-full md:w-64 text-sm font-medium"
                />
              </div>
              <button className="flex items-center justify-center gap-2 bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg transition-colors text-sm font-bold">
                <UserPlus size={18} /> Add User
              </button>
            </div>
            <table className="w-full text-left">
              <thead className="bg-slate-800/50 text-slate-400 text-[11px] uppercase font-bold tracking-widest">
                <tr>
                  <th className="px-6 py-4">User</th>
                  <th className="px-6 py-4">Plan</th>
                  <th className="px-6 py-4">Status</th>
                  <th className="px-6 py-4 text-center">Win Rate</th>
                  <th className="px-6 py-4">Last Login</th>
                  <th className="px-6 py-4 text-right">Actions</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-slate-800">
                {MOCK_USERS.map(user => (
                  <tr key={user.id} className="text-slate-300 hover:bg-slate-800/30 transition-colors">
                    <td className="px-6 py-4">
                      <div className="font-semibold text-white text-sm">{user.name}</div>
                      <div className="text-xs text-slate-500 mt-0.5">{user.email}</div>
                    </td>
                    <td className="px-6 py-4">
                      <span className={`px-2 py-0.5 rounded text-[10px] font-black uppercase tracking-tighter ${user.plan === 'Enterprise' ? 'bg-amber-500/10 text-amber-400' : 'bg-slate-700 text-slate-300'}`}>
                        {user.plan}
                      </span>
                    </td>
                    <td className="px-6 py-4">
                      <div className="flex items-center gap-2 text-xs font-bold uppercase tracking-tight">
                        <div className={`w-1.5 h-1.5 rounded-full ${user.status === 'Active' ? 'bg-emerald-500' : user.status === 'Banned' ? 'bg-rose-500' : 'bg-slate-500'}`} />
                        {user.status}
                      </div>
                    </td>
                    <td className="px-6 py-4 font-mono text-sm text-center font-bold text-indigo-400">{user.winRate}</td>
                    <td className="px-6 py-4 text-slate-500 text-xs font-medium">{user.lastLogin}</td>
                    <td className="px-6 py-4 text-right">
                      <button className="text-slate-500 hover:text-white"><MoreVertical size={18} /></button>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        );

      case 'blog':
        return (
          <div className="space-y-6">
            <div className="flex justify-between items-center">
              <h2 className="text-2xl font-black text-white tracking-tighter">Blogpost Manager</h2>
              <button className="flex items-center gap-2 bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg transition-colors text-sm font-bold">
                <Plus size={18} /> New Post
              </button>
            </div>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              {MOCK_POSTS.map(post => (
                <div key={post.id} className="bg-slate-900 border border-slate-800 p-5 rounded-xl flex flex-col justify-between hover:border-slate-700 transition-colors">
                  <div>
                    <div className="flex justify-between items-start mb-3">
                      <span className={`text-[10px] uppercase font-black tracking-widest px-2 py-0.5 rounded ${post.status === 'Published' ? 'bg-emerald-500/10 text-emerald-400' : 'bg-slate-700 text-slate-300'}`}>
                        {post.status}
                      </span>
                      <span className="text-xs text-slate-500 font-bold">{post.date}</span>
                    </div>
                    <h4 className="text-lg font-bold text-white mb-2 leading-tight tracking-tight">{post.title}</h4>
                    <p className="text-slate-400 text-sm line-clamp-2 mb-4 font-medium leading-relaxed">
                      Exploring the impact of the latest Consumer Price Index data on major FX pairs and the volatility expected...
                    </p>
                  </div>
                  <div className="flex justify-between items-center pt-4 border-t border-slate-800">
                    <span className="text-xs text-slate-500 font-semibold tracking-tight">By {post.author}</span>
                    <button className="text-indigo-400 text-xs font-bold uppercase tracking-widest hover:underline">Edit Content</button>
                  </div>
                </div>
              ))}
            </div>
          </div>
        );

      case 'marketing':
        return (
          <div className="max-w-4xl space-y-8">
            <div className="bg-slate-900 border border-slate-800 rounded-xl p-8">
              <h3 className="text-xl font-black text-white mb-6 flex items-center gap-2 tracking-tight">
                <Megaphone className="text-indigo-400" /> Multi-Channel Broadcast
              </h3>
              <div className="space-y-4">
                <div>
                  <label className="block text-xs font-black uppercase tracking-widest text-slate-500 mb-2">Target Audience</label>
                  <select className="w-full bg-slate-800 border border-slate-700 text-white rounded-lg p-3 outline-none focus:border-indigo-500 text-sm font-bold">
                    <option>All Users (12,842)</option>
                    <option>Free Plan Only</option>
                    <option>Inactive Users ({'>'} 30 days)</option>
                    <option>Pro Traders</option>
                  </select>
                </div>
                <div>
                  <label className="block text-xs font-black uppercase tracking-widest text-slate-500 mb-2">Communication Method</label>
                  <div className="grid grid-cols-3 gap-3">
                    <button className="flex flex-col items-center gap-2 p-4 border border-indigo-500/50 bg-indigo-500/10 rounded-xl text-indigo-400">
                      <Mail size={24} /> <span className="text-[10px] font-black uppercase tracking-widest">Email</span>
                    </button>
                    <button className="flex flex-col items-center gap-2 p-4 border border-slate-700 bg-slate-800/50 rounded-xl text-slate-400 hover:border-slate-500 transition-colors">
                      <AlertCircle size={24} /> <span className="text-[10px] font-black uppercase tracking-widest">Push</span>
                    </button>
                    <button className="flex flex-col items-center gap-2 p-4 border border-slate-700 bg-slate-800/50 rounded-xl text-slate-400 hover:border-slate-500 transition-colors">
                      <ShieldCheck size={24} /> <span className="text-[10px] font-black uppercase tracking-widest">In-App</span>
                    </button>
                  </div>
                </div>
                <div>
                  <label className="block text-xs font-black uppercase tracking-widest text-slate-500 mb-2">Message Content</label>
                  <textarea 
                    rows="4"
                    className="w-full bg-slate-800 border border-slate-700 text-white rounded-lg p-3 outline-none focus:border-indigo-500 placeholder:text-slate-600 text-sm font-medium"
                    placeholder="Enter your announcement or promo code here..."
                  ></textarea>
                </div>
                <button className="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-black py-4 rounded-lg shadow-lg transition-all uppercase tracking-[0.2em] text-xs">
                  Send Campaign Now
                </button>
              </div>
            </div>
            
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div className="bg-slate-900/50 border border-slate-800 p-6 rounded-xl flex items-center justify-between hover:border-slate-600 transition-colors cursor-pointer group">
                <div>
                  <h4 className="text-white font-bold tracking-tight">Active Promos</h4>
                  <p className="text-slate-500 text-xs font-semibold">SUMMER25 - 25% off Yearly</p>
                </div>
                <ChevronRight className="text-slate-600 group-hover:text-white transition-colors" />
              </div>
              <div className="bg-slate-900/50 border border-slate-800 p-6 rounded-xl flex items-center justify-between hover:border-slate-600 transition-colors cursor-pointer group">
                <div>
                  <h4 className="text-white font-bold tracking-tight">Email Templates</h4>
                  <p className="text-slate-500 text-xs font-semibold">6 Saved layouts</p>
                </div>
                <ChevronRight className="text-slate-600 group-hover:text-white transition-colors" />
              </div>
            </div>
          </div>
        );

      default:
        return <div className="p-12 text-center text-slate-500 italic font-medium">Section coming soon...</div>;
    }
  };

  return (
    <div className="flex h-screen bg-slate-950 text-slate-100 overflow-hidden font-['Inter',_ui-sans-serif,_system-ui,_-apple-system,_BlinkMacSystemFont,_'Segoe_UI',_Roboto,_'Helvetica_Neue',_Arial,_'Noto_Sans',_sans-serif]">
      {/* Google Font Import */}
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');
      `}</style>
      
      {/* Sidebar */}
      <aside className="w-64 bg-slate-900 border-r border-slate-800 flex flex-col">
        <div className="p-6">
          <div className="flex items-center gap-3 text-indigo-500 mb-8">
            <div className="bg-indigo-600 p-2 rounded-lg shadow-inner">
              <LayoutDashboard size={24} className="text-white" />
            </div>
            <span className="font-black text-xl tracking-tighter text-white uppercase">FSD ADMIN</span>
          </div>
          
          <nav className="space-y-2">
            <SidebarItem id="dashboard" label="Overview" icon={LayoutDashboard} active={activeTab === 'dashboard'} onClick={setActiveTab} />
            <SidebarItem id="users" label="User Accounts" icon={Users} active={activeTab === 'users'} onClick={setActiveTab} />
            <SidebarItem id="analytics" label="Trade Analysis" icon={LineChart} active={activeTab === 'analytics'} onClick={setActiveTab} />
            <SidebarItem id="blog" label="Blogpost" icon={FileText} active={activeTab === 'blog'} onClick={setActiveTab} />
            <SidebarItem id="marketing" label="Marketing" icon={Megaphone} active={activeTab === 'marketing'} onClick={setActiveTab} />
          </nav>
        </div>
        
        <div className="mt-auto p-6 border-t border-slate-800">
          <SidebarItem id="settings" label="System Settings" icon={Settings} active={activeTab === 'settings'} onClick={setActiveTab} />
          <div className="mt-6 flex items-center gap-3 p-2 bg-slate-800/50 rounded-lg">
            <div className="w-8 h-8 rounded-full bg-indigo-500 flex items-center justify-center font-black text-[10px] shadow-sm">AD</div>
            <div className="flex-1 min-w-0">
              <p className="text-sm font-bold truncate tracking-tight">Admin User</p>
              <p className="text-[9px] text-slate-500 uppercase font-black tracking-widest">Super Admin</p>
            </div>
          </div>
        </div>
      </aside>

      {/* Main Content Area */}
      <main className="flex-1 overflow-y-auto bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-slate-900 via-slate-950 to-slate-950">
        <header className="sticky top-0 z-10 bg-slate-950/80 backdrop-blur-md border-b border-slate-800 px-8 py-4 flex justify-between items-center">
          <h1 className="text-xl font-black capitalize tracking-tight">{activeTab === 'blog' ? 'Blogpost' : activeTab.replace('-', ' ')}</h1>
          <div className="flex items-center gap-4">
            <button className="relative p-2 text-slate-400 hover:text-white transition-colors">
              <Mail size={20} />
              <span className="absolute top-1.5 right-1.5 w-2 h-2 bg-rose-500 rounded-full border-2 border-slate-950 shadow-sm" />
            </button>
          </div>
        </header>

        <section className="p-8">
          {renderContent()}
        </section>
      </main>
    </div>
  );
}
