import React, { useState, useEffect } from 'react';
import { 
  TrendingUp, 
  Globe, 
  BarChart3, 
  Coins, 
  ShieldCheck, 
  Clock, 
  Calendar, 
  BookOpen, 
  ArrowRight,
  ChevronRight,
  Menu,
  X,
  Target,
  Zap,
  Book,
  BellRing,
  Twitter,
  Youtube,
  Instagram,
  Mail,
  Facebook,
  Search,
  Sun,
  Moon
} from 'lucide-react';

// Custom SVG for Telegram to ensure brand accuracy
const TelegramIcon = ({ size = 18, color = "currentColor" }) => (
  <svg 
    width={size} 
    height={size} 
    viewBox="0 0 24 24" 
    fill="none" 
    stroke={color} 
    strokeWidth="2" 
    strokeLinecap="round" 
    strokeLinejoin="round"
  >
    <path d="m22 2-7 20-4-9-9-4Z" />
    <path d="M22 2 11 13" />
  </svg>
);

const App = () => {
  const [activeTab, setActiveTab] = useState('Major Pairs');
  const [scrolled, setScrolled] = useState(false);
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [theme, setTheme] = useState('dark'); // 'dark' or 'light'

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const toggleTheme = () => {
    setTheme(prev => prev === 'dark' ? 'light' : 'dark');
  };

  const marketPages = [
    { 
      id: 'Major Pairs', 
      icon: <Globe size={20} />, 
      desc: 'Focusing on EURUSD & GBPUSD institutional order blocks.',
      data: { trend: 'Bullish', volatility: 'Med', liquidity: 'Extreme' }
    },
    { 
      id: 'US Stocks', 
      icon: <TrendingUp size={20} />, 
      desc: 'NASDAQ & S&P500 intraday scalping zones.',
      data: { trend: 'Neutral', volatility: 'High', liquidity: 'High' }
    },
    { 
      id: 'Commodities', 
      icon: <BarChart3 size={20} />, 
      desc: 'Gold (XAU) and Crude Oil supply/demand imbalances.',
      data: { trend: 'Bullish', volatility: 'V. High', liquidity: 'Med' }
    },
    { 
      id: 'Cryptocurrency', 
      icon: <Coins size={20} />, 
      desc: 'Bitcoin and Ethereum liquidation heatmaps.',
      data: { trend: 'Bearish', volatility: 'Extreme', liquidity: 'Low' }
    },
    { 
      id: 'Verified Strategies', 
      icon: <ShieldCheck size={20} />, 
      desc: 'Backtested blueprints with defined R:R metrics.',
      data: { winrate: '64%', avgRR: '1:3.2', tested: '5Y' }
    }
  ];

  // Dynamic Styles based on theme
  const isDark = theme === 'dark';
  const bgColor = isDark ? 'bg-[#050505]' : 'bg-[#fcfcfc]';
  const textColor = isDark ? 'text-[#e0e0e0]' : 'text-[#222222]';
  const borderColor = isDark ? 'border-white/10' : 'border-black/10';
  const navBg = isDark ? (scrolled ? 'bg-black/95' : 'bg-transparent') : (scrolled ? 'bg-white/95' : 'bg-transparent');
  const subNavBg = isDark ? 'bg-white/[0.02]' : 'bg-black/[0.02]';
  const sectionBorder = isDark ? 'border-white/5' : 'border-black/5';

  return (
    <div className={`min-h-screen ${bgColor} ${textColor} font-mono selection:bg-emerald-500/30 overflow-x-hidden transition-colors duration-300`}>
      {/* Navigation System */}
      <nav className={`fixed w-full z-50 transition-all duration-500 ${navBg} ${scrolled ? 'shadow-2xl' : ''}`}>
        {/* Top Utility Bar */}
        <div className={`border-b ${sectionBorder} ${isDark ? 'bg-black/40' : 'bg-white/40'} backdrop-blur-md`}>
          <div className="max-w-7xl mx-auto px-6 py-2 flex justify-between items-center text-[10px] font-bold uppercase tracking-widest">
            <div className="flex items-center gap-6">
              <a href="#" className={`flex items-center gap-2 transition-colors group ${isDark ? 'hover:text-white' : 'hover:text-black'}`}>
                <TelegramIcon size={12} color="#229ED9" /> <span className="group-hover:text-[#229ED9]">Join Telegram</span>
              </a>
              <a href="#" className="flex items-center gap-2 hover:text-emerald-500 transition-colors">
                <BellRing size={12} /> Subscribe
              </a>
            </div>
            <div className="flex items-center gap-6">
              {/* Theme Toggle Button */}
              <button 
                onClick={toggleTheme}
                className={`flex items-center gap-2 p-1 px-2 rounded-full border transition-all ${isDark ? 'border-white/10 hover:bg-white/5 text-slate-400' : 'border-black/10 hover:bg-black/5 text-slate-600'}`}
              >
                {isDark ? <Sun size={10} /> : <Moon size={10} />}
                <span className="text-[8px]">{isDark ? 'LIGHT' : 'DARK'}</span>
              </button>
              
              <span className={isDark ? 'text-white/20' : 'text-black/20'}>|</span>
              <div className="flex gap-4">
                <a href="#" className="hover:text-emerald-500 transition-colors">Sign In</a>
                <a href="#" className="text-emerald-500 hover:text-emerald-400 transition-colors">Sign Up</a>
              </div>
            </div>
          </div>
        </div>

        {/* Main Nav Bar */}
        <div className={`border-b ${borderColor}`}>
          <div className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
            <div className={`text-2xl font-black tracking-tighter flex items-center ${isDark ? 'text-white' : 'text-black'}`}>
              FSDZONES<span className="text-emerald-500">.COM</span>
            </div>

            <div className="hidden lg:flex items-center space-x-8 text-[10px] font-black uppercase tracking-[0.15em]">
              <a href="#" className="text-emerald-500">Home</a>
              <a href="#journal" className="hover:text-emerald-500 transition-colors">Free Journal</a>
              <a href="#tsc" className="hover:text-emerald-500 transition-colors">TSC</a>
              <a href="#calendar" className="hover:text-emerald-500 transition-colors">Economic Calendar</a>
              <a href="#blog" className="hover:text-emerald-500 transition-colors">Blog</a>
            </div>

            <button className={`lg:hidden ${isDark ? 'text-white' : 'text-black'}`} onClick={() => setIsMenuOpen(!isMenuOpen)}>
              {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
            </button>
          </div>
        </div>

        {/* Secondary Sub-nav for Categories */}
        <div className={`${subNavBg} border-b ${sectionBorder} hidden lg:block overflow-x-auto no-scrollbar`}>
          <div className={`max-w-7xl mx-auto px-6 py-3 flex items-center justify-center space-x-10 text-[9px] font-bold uppercase tracking-[0.2em] ${isDark ? 'text-slate-500' : 'text-slate-400'}`}>
            {marketPages.map(page => (
              <a 
                key={page.id} 
                href={`#${page.id.replace(/\s+/g, '-').toLowerCase()}`}
                onClick={(e) => {
                    e.preventDefault();
                    setActiveTab(page.id);
                }}
                className={`hover:text-emerald-500 transition-colors flex items-center gap-2 ${activeTab === page.id ? (isDark ? 'text-white border-b border-emerald-500' : 'text-black border-b border-emerald-500') : ''} pb-0.5`}
              >
                {page.id}
              </a>
            ))}
          </div>
        </div>
      </nav>

      {/* Hero / Spacer */}
      <header className="relative pt-44 pb-0 px-6">
        <div className="max-w-7xl mx-auto">
        </div>
      </header>

      {/* Global Terminal Search Bar */}
      <section className="px-6 py-12">
        <div className="max-w-7xl mx-auto">
          <div className="relative group">
            <div className="absolute inset-y-0 left-6 flex items-center pointer-events-none">
              <Search className={`text-emerald-500 group-focus-within:${isDark ? 'text-white' : 'text-black'} transition-colors`} size={20} />
            </div>
            <input 
              type="text" 
              placeholder="SEARCH SYMBOLS, STRATEGIES, OR LIQUIDITY ZONES..." 
              className={`w-full ${isDark ? 'bg-white/[0.03] text-white focus:bg-white/[0.05]' : 'bg-black/[0.03] text-black focus:bg-black/[0.05]'} border ${borderColor} py-6 pl-16 pr-6 text-xs font-black uppercase tracking-[0.2em] focus:outline-none focus:border-emerald-500/50 transition-all placeholder:text-slate-500`}
            />
            <div className={`absolute inset-y-0 right-6 flex items-center gap-4 text-[10px] font-bold uppercase tracking-widest hidden md:flex pointer-events-none ${isDark ? 'text-slate-600' : 'text-slate-400'}`}>
              <span>Press</span>
              <span className={`px-2 py-1 border rounded ${isDark ? 'bg-white/5 border-white/10' : 'bg-black/5 border-black/10'}`}>CMD + K</span>
            </div>
          </div>
        </div>
      </section>

      {/* Highlights Section */}
      <section className={`py-4 px-6 border-b ${sectionBorder}`}>
        <div className="max-w-7xl mx-auto">
          <div className={`flex items-center gap-4 mb-8 uppercase tracking-[0.3em] text-[10px] font-black ${isDark ? 'text-slate-500' : 'text-slate-400'}`}>
            <div className="w-8 h-[1px] bg-emerald-500"></div>
            Market Overview
          </div>
          <div className={`grid lg:grid-cols-5 gap-px ${isDark ? 'bg-white/10' : 'bg-black/10'} border ${borderColor} overflow-hidden transition-colors`}>
            {marketPages.map((page) => (
              <div 
                key={page.id}
                id={page.id.replace(/\s+/g, '-').toLowerCase()}
                onMouseEnter={() => setActiveTab(page.id)}
                className={`p-8 cursor-pointer transition-all duration-300 relative ${bgColor}
                  ${activeTab === page.id ? 'z-10' : ''}
                `}
              >
                {activeTab === page.id && <div className="absolute inset-0 border-2 border-emerald-500 pointer-events-none"></div>}
                
                <div className={`mb-6 transition-colors ${activeTab === page.id ? 'text-emerald-500' : (isDark ? 'text-slate-400' : 'text-slate-500')}`}>
                  {page.icon}
                </div>
                <h3 className={`text-xs font-black uppercase tracking-widest mb-3 ${isDark ? 'text-white' : 'text-black'}`}>{page.id}</h3>
                <p className={`text-[11px] leading-relaxed mb-6 h-12 font-sans ${isDark ? 'text-slate-300' : 'text-slate-600'}`}>{page.desc}</p>
                
                <div className={`space-y-2 border-t ${borderColor} pt-4`}>
                  {Object.entries(page.data).map(([key, val]) => (
                    <div key={key} className="flex justify-between text-[10px] uppercase tracking-tighter">
                      <span className={`${isDark ? 'text-slate-400' : 'text-slate-500'} font-bold`}>{key}</span>
                      <span className={`${isDark ? 'text-white' : 'text-black'} font-black`}>{val}</span>
                    </div>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Utilities */}
      <section className={`py-24 px-6 border-b ${sectionBorder}`}>
        <div className={`max-w-7xl mx-auto grid lg:grid-cols-3 gap-px ${isDark ? 'bg-white/10 border-white/10' : 'bg-black/10 border-black/10'} border`}>
          <div id="journal" className={`${bgColor} p-10 ${isDark ? 'hover:bg-white/[0.02]' : 'hover:bg-black/[0.02]'} transition-colors group`}>
            <BookOpen className="text-emerald-500 mb-8" size={32} />
            <h2 className={`text-2xl font-black uppercase tracking-tighter mb-4 ${isDark ? 'text-white' : 'text-black'}`}>Free Journal</h2>
            <p className={`${isDark ? 'text-slate-400' : 'text-slate-500'} text-xs font-sans leading-relaxed mb-8`}>Professional trade logging with performance metrics and emotional tracking. 100% Free.</p>
            <button className={`flex items-center gap-2 text-[10px] font-black uppercase tracking-widest group-hover:text-emerald-500 transition-colors ${isDark ? 'text-white' : 'text-black'}`}>
              Access Journal <ArrowRight size={14} />
            </button>
          </div>

          <div id="tsc" className={`${bgColor} p-10 ${isDark ? 'hover:bg-white/[0.02]' : 'hover:bg-black/[0.02]'} transition-colors group`}>
            <Clock className="text-emerald-500 mb-8" size={32} />
            <h2 className={`text-2xl font-black uppercase tracking-tighter mb-4 ${isDark ? 'text-white' : 'text-black'}`}>TSC</h2>
            <p className={`${isDark ? 'text-slate-400' : 'text-slate-500'} text-xs font-sans leading-relaxed mb-8`}>Real-time Trading Session Clock tracking London, NY, and Tokyo liquidity cycles.</p>
            <button className={`flex items-center gap-2 text-[10px] font-black uppercase tracking-widest group-hover:text-emerald-500 transition-colors ${isDark ? 'text-white' : 'text-black'}`}>
              Launch Clock <ArrowRight size={14} />
            </button>
          </div>

          <div id="calendar" className={`${bgColor} p-10 ${isDark ? 'hover:bg-white/[0.02]' : 'hover:bg-black/[0.02]'} transition-colors group`}>
            <Calendar className="text-emerald-500 mb-8" size={32} />
            <h2 className={`text-2xl font-black uppercase tracking-tighter mb-4 ${isDark ? 'text-white' : 'text-black'}`}>Economic Calendar</h2>
            <p className={`${isDark ? 'text-slate-400' : 'text-slate-500'} text-xs font-sans leading-relaxed mb-8`}>High-impact news filtering and macro data countdowns for intraday precision.</p>
            <button className={`flex items-center gap-2 text-[10px] font-black uppercase tracking-widest group-hover:text-emerald-500 transition-colors ${isDark ? 'text-white' : 'text-black'}`}>
              Check Calendar <ArrowRight size={14} />
            </button>
          </div>
        </div>
      </section>

      {/* Blog */}
      <section id="blog" className="py-24 px-6">
        <div className="max-w-7xl mx-auto">
          <div className="flex justify-between items-end mb-12">
            <h2 className={`text-3xl font-black uppercase tracking-tighter flex items-center gap-4 ${isDark ? 'text-white' : 'text-black'}`}>
              <Book className="text-emerald-500" /> Blog
            </h2>
            <a href="#" className="text-[10px] font-black uppercase tracking-[0.2em] border-b border-emerald-500 pb-1">View Archives</a>
          </div>

          <div className={`space-y-px ${isDark ? 'bg-white/10' : 'bg-black/10'}`}>
            {[
              { cat: 'Psychology', title: 'Navigating Drawdown with Neutrality', date: 'JAN 24' },
              { cat: 'Technical', title: 'Why S/R Zones Get Swept', date: 'JAN 21' },
              { cat: 'Macro', title: 'Interest Rate Pivot: 2024 Outlook', date: 'JAN 18' },
            ].map((post, idx) => (
              <div key={idx} className={`group flex items-center justify-between p-8 ${bgColor} ${isDark ? 'hover:bg-white/[0.02]' : 'hover:bg-black/[0.02]'} transition-all cursor-pointer`}>
                <div className="flex items-center gap-12">
                  <span className={`text-[9px] font-black uppercase w-20 ${isDark ? 'text-slate-500' : 'text-slate-400'}`}>{post.cat}</span>
                  <h4 className={`text-lg font-bold group-hover:text-emerald-500 transition-colors ${isDark ? 'text-white' : 'text-black'}`}>{post.title}</h4>
                </div>
                <span className={`text-[9px] font-mono uppercase ${isDark ? 'text-slate-600' : 'text-slate-400'}`}>{post.date}</span>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Advanced Footer */}
      <footer className={`${isDark ? 'bg-black' : 'bg-[#f5f5f5]'} border-t ${borderColor} pt-20 pb-10 px-6 overflow-hidden relative transition-colors`}>
        <div className="max-w-7xl mx-auto relative z-10">
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mb-20">
            {/* Column 1: Brand */}
            <div className="space-y-6">
              <div className={`text-2xl font-black tracking-tighter ${isDark ? 'text-white' : 'text-black'}`}>
                FSDZONES<span className="text-emerald-500">.COM</span>
              </div>
              <div className="flex items-center gap-4 pt-2">
                <a href="#" className={`${isDark ? 'text-slate-400 hover:text-[#1DA1F2]' : 'text-slate-500 hover:text-[#1DA1F2]'} transition-colors`}><Twitter size={18} fill="currentColor" stroke="none" /></a>
                <a href="#" className={`${isDark ? 'text-slate-400 hover:text-[#FF0000]' : 'text-slate-500 hover:text-[#FF0000]'} transition-colors`}><Youtube size={18} /></a>
                <a href="#" className={`${isDark ? 'text-slate-400 hover:text-[#229ED9]' : 'text-slate-500 hover:text-[#229ED9]'} transition-colors`} title="Telegram"><TelegramIcon size={18} color="inherit" /></a>
                <a href="#" className={`${isDark ? 'text-slate-400 hover:text-[#1877F2]' : 'text-slate-500 hover:text-[#1877F2]'} transition-colors`}><Facebook size={18} fill="currentColor" stroke="none" /></a>
                <a href="#" className={`${isDark ? 'text-slate-400 hover:text-[#E4405F]' : 'text-slate-500 hover:text-[#E4405F]'} transition-colors`}><Instagram size={18} /></a>
              </div>
            </div>

            {/* Column 2: Markets */}
            <div>
              <h4 className={`text-[10px] font-black uppercase tracking-[0.3em] mb-8 ${isDark ? 'text-white' : 'text-black'}`}>Markets</h4>
              <ul className={`space-y-4 text-[10px] font-bold uppercase tracking-widest ${isDark ? 'text-slate-500 hover:text-white' : 'text-slate-400 hover:text-black'}`}>
                <li><a href="#" className="hover:text-emerald-500 transition-colors">Major Forex Pairs</a></li>
                <li><a href="#" className="hover:text-emerald-500 transition-colors">US Indices (NAS/SPX)</a></li>
                <li><a href="#" className="hover:text-emerald-500 transition-colors">Commodities (Gold/Oil)</a></li>
                <li><a href="#" className="hover:text-emerald-500 transition-colors">Cryptocurrency</a></li>
                <li><a href="#" className="hover:text-emerald-500 transition-colors">Institutional Order Flow</a></li>
              </ul>
            </div>

            {/* Column 3: Resources */}
            <div>
              <h4 className={`text-[10px] font-black uppercase tracking-[0.3em] mb-8 ${isDark ? 'text-white' : 'text-black'}`}>Resources</h4>
              <ul className={`space-y-4 text-[10px] font-bold uppercase tracking-widest ${isDark ? 'text-slate-500 hover:text-white' : 'text-slate-400 hover:text-black'}`}>
                <li><a href="#journal" className="hover:text-emerald-500 transition-colors">Free Trading Journal</a></li>
                <li><a href="#tsc" className="hover:text-emerald-500 transition-colors">Session Clock (TSC)</a></li>
                <li><a href="#calendar" className="hover:text-emerald-500 transition-colors">Economic Calendar</a></li>
                <li><a href="#blog" className="hover:text-emerald-500 transition-colors">Strategy Blog</a></li>
                <li><a href="#" className="hover:text-emerald-500 transition-colors">Backtesting Data</a></li>
              </ul>
            </div>

            {/* Column 4: Newsletter */}
            <div>
              <h4 className={`text-[10px] font-black uppercase tracking-[0.3em] mb-8 ${isDark ? 'text-white' : 'text-black'}`}>Stay Updated</h4>
              <p className={`text-[10px] mb-6 uppercase tracking-wider ${isDark ? 'text-slate-500' : 'text-slate-400'}`}>Get intraday zone alerts and macro updates directly to your inbox.</p>
              <div className={`flex border p-1 ${isDark ? 'bg-white/5 border-white/10' : 'bg-black/5 border-black/10'}`}>
                <input 
                  type="email" 
                  placeholder="EMAIL ADDRESS" 
                  className={`bg-transparent border-none text-[10px] p-2 flex-grow focus:outline-none ${isDark ? 'text-white' : 'text-black'} placeholder:text-slate-500 font-bold`}
                />
                <button className="bg-emerald-500 text-black px-4 text-[10px] font-black uppercase hover:bg-emerald-400 transition-colors">
                  Join
                </button>
              </div>
            </div>
          </div>

          {/* Legal & Bottom Bar */}
          <div className={`pt-10 border-t ${isDark ? 'border-white/5' : 'border-black/5'}`}>
            <div className="flex flex-col lg:flex-row justify-between items-start lg:items-center gap-8">
              <div className="space-y-2">
                <div className={`text-[9px] uppercase tracking-[0.2em] font-bold ${isDark ? 'text-slate-600' : 'text-slate-500'}`}>
                  © 2024 FSDZONES.COM | ALL RIGHTS RESERVED
                </div>
                <div className={`text-[8px] max-w-2xl uppercase leading-relaxed font-bold ${isDark ? 'text-slate-700' : 'text-slate-400'}`}>
                  Risk Warning: Trading financial markets involves significant risk. FSDZONES provides educational content and data analytics for informational purposes only. Past performance is not indicative of future results. Never trade with money you cannot afford to lose.
                </div>
              </div>
              <div className={`flex gap-6 text-[9px] font-bold uppercase tracking-widest ${isDark ? 'text-slate-500' : 'text-slate-400'}`}>
                <a href="#" className="hover:text-emerald-500 transition-colors">Privacy Policy</a>
                <a href="#" className="hover:text-white transition-colors">Terms of Service</a>
                <a href="#" className="hover:text-white transition-colors">Contact</a>
              </div>
            </div>
          </div>
        </div>

        {/* Decorative background element */}
        <div className={`absolute top-0 right-0 w-96 h-96 blur-[120px] rounded-full -mr-48 -mt-48 ${isDark ? 'bg-emerald-500/5' : 'bg-emerald-500/10'}`}></div>
      </footer>
    </div>
  );
};

export default App;
