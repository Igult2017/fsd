import { useState, useEffect } from "react";

// ─── Design Tokens ────────────────────────────────────────────────────────────
const colors = {
  primary: "#004AC6",
  primaryContainer: "#2563eb",
  onPrimary: "#ffffff",
  background: "#f8f9ff",
  surface: "#f8f9ff",
  onBackground: "#0b1c30",
  secondary: "#565e74",
  outlineVariant: "#c3c6d7",
  tertiary: "#006242",
  error: "#ba1a1a",
  surfaceContainerLow: "#eff4ff",
  neonGreen: "#00ff88",
};

// Shared inner container — identical horizontal bounds on every section and the navbar
const INNER = { maxWidth: 1280, margin: "0 auto", paddingLeft: 32, paddingRight: 32 };
const INNER_MOBILE = { maxWidth: 1280, margin: "0 auto", paddingLeft: 16, paddingRight: 16 };

function inner(isMobile) {
  return isMobile ? INNER_MOBILE : INNER;
}

// ─── Responsive Hook ──────────────────────────────────────────────────────────
function useBreakpoint() {
  const [w, setW] = useState(() => (typeof window !== "undefined" ? window.innerWidth : 1200));
  useEffect(() => {
    const handler = () => setW(window.innerWidth);
    window.addEventListener("resize", handler);
    return () => window.removeEventListener("resize", handler);
  }, []);
  return { isMobile: w < 640, isTablet: w >= 640 && w < 1024, isDesktop: w >= 1024, w };
}

// ─── Shared Components ────────────────────────────────────────────────────────
function GlassCard({ children, style: s = {}, hover = true }) {
  const [hovered, setHovered] = useState(false);
  return (
    <div
      onMouseEnter={() => setHovered(true)}
      onMouseLeave={() => setHovered(false)}
      style={{
        background: "rgba(255,255,255,0.7)",
        backdropFilter: "blur(12px)",
        WebkitBackdropFilter: "blur(12px)",
        border: "1px solid rgba(226,232,240,0.8)",
        boxShadow: "0 4px 30px rgba(0,0,0,0.03), inset 0 0 0 1px rgba(255,255,255,0.5)",
        transform: hover && hovered ? "translateY(-4px)" : "translateY(0)",
        transition: "all 0.4s cubic-bezier(0.4,0,0.2,1)",
        ...s,
      }}
    >
      {children}
    </div>
  );
}

function Mono({ children, style: s = {} }) {
  return <span style={{ fontFamily: "'DM Mono', monospace", ...s }}>{children}</span>;
}

function Logo({ size = 22 }) {
  return (
    <span style={{ fontSize: size, lineHeight: 1, fontFamily: "'Montserrat', sans-serif" }}>
      <span style={{ fontWeight: 800, color: colors.onBackground }}>Myfm</span>
      <span style={{ fontWeight: 400, color: colors.primary }}>journal</span>
    </span>
  );
}

function Stars({ size = 16 }) {
  return (
    <span style={{ display: "inline-flex", gap: 2 }}>
      {[...Array(5)].map((_, i) => (
        <svg key={i} width={size} height={size} viewBox="0 0 24 24" fill="#FFA000">
          <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z" />
        </svg>
      ))}
    </span>
  );
}

const DASHED_BORDER = `url("data:image/svg+xml,%3csvg width='100%25' height='100%25' xmlns='http://www.w3.org/2000/svg'%3e%3crect width='100%25' height='100%25' fill='none' rx='9999' ry='9999' stroke='%23004AC6FF' stroke-width='2' stroke-dasharray='6%2c 14' stroke-dashoffset='0' stroke-linecap='square'/%3e%3c/svg%3e")`;

// ─── Navbar ───────────────────────────────────────────────────────────────────
function Navbar() {
  const [scrolled, setScrolled] = useState(false);
  const [menuOpen, setMenuOpen] = useState(false);
  const { isMobile, isTablet } = useBreakpoint();
  const isSmall = isMobile || isTablet;

  useEffect(() => {
    const h = () => setScrolled(window.scrollY > 20);
    window.addEventListener("scroll", h);
    return () => window.removeEventListener("scroll", h);
  }, []);

  const links = ["Features", "Pricing", "Reviews", "Economic Calendar", "Blog", "Sessions"];

  return (
    <header style={{
      position: "fixed", top: 0, left: 0, right: 0, zIndex: 100,
      background: scrolled ? "rgba(248,249,255,0.97)" : "rgba(248,249,255,0.88)",
      backdropFilter: "blur(14px)", WebkitBackdropFilter: "blur(14px)",
      borderBottom: "1px solid rgba(195,198,215,0.3)",
      boxShadow: scrolled ? "0 4px 20px rgba(0,0,0,0.06)" : "none",
      transition: "all 0.3s ease",
      fontFamily: "'Montserrat', sans-serif",
    }}>
      {/* Nav inner — defines the left/right boundary everything else aligns to */}
      <nav style={{
        ...inner(isMobile),
        display: "flex", justifyContent: "space-between", alignItems: "center",
        height: isMobile ? 56 : 60,
      }}>
        <div style={{ flexShrink: 0 }}>
          <Logo size={isMobile ? 17 : 20} />
        </div>

        {!isSmall && (
          <div style={{ display: "flex", gap: 24, alignItems: "center" }}>
            {links.map(item => (
              <a key={item} href={`#${item.toLowerCase().replace(" ", "")}`}
                style={{ color: colors.secondary, fontWeight: 700, fontSize: 11, letterSpacing: "0.05em", textDecoration: "none", textTransform: "uppercase", transition: "color 0.2s" }}
                onMouseEnter={e => e.target.style.color = colors.primary}
                onMouseLeave={e => e.target.style.color = colors.secondary}>
                {item}
              </a>
            ))}
          </div>
        )}

        <div style={{ display: "flex", gap: 10, alignItems: "center" }}>
          {!isSmall && (
            <button style={{ background: "none", border: "none", color: colors.secondary, fontWeight: 700, fontSize: 11, letterSpacing: "0.05em", textTransform: "uppercase", cursor: "pointer", fontFamily: "'Montserrat', sans-serif" }}>
              Login
            </button>
          )}
          <button style={{ background: colors.primary, color: "#fff", border: "none", borderRadius: 7, padding: isMobile ? "7px 14px" : "7px 18px", fontWeight: 800, fontSize: 11, letterSpacing: "0.05em", textTransform: "uppercase", cursor: "pointer", boxShadow: "0 4px 14px rgba(0,74,198,0.3)", fontFamily: "'Montserrat', sans-serif" }}>
            Signup
          </button>
          {isSmall && (
            <button onClick={() => setMenuOpen(o => !o)} style={{ background: "none", border: "none", cursor: "pointer", padding: 6, display: "flex", flexDirection: "column", gap: 5 }}>
              {[0, 1, 2].map(i => (
                <span key={i} style={{ display: "block", width: 20, height: 2, background: colors.onBackground, borderRadius: 2, transition: "all 0.3s",
                  transform: menuOpen ? (i === 0 ? "rotate(45deg) translate(5px,5px)" : i === 2 ? "rotate(-45deg) translate(5px,-5px)" : "scaleX(0)") : "none",
                  opacity: menuOpen && i === 1 ? 0 : 1,
                }} />
              ))}
            </button>
          )}
        </div>
      </nav>

      {isSmall && menuOpen && (
        <div style={{ ...inner(isMobile), paddingTop: 12, paddingBottom: 18, display: "flex", flexDirection: "column", gap: 2, borderTop: "1px solid rgba(195,198,215,0.3)", background: "rgba(248,249,255,0.98)" }}>
          {links.map(item => (
            <a key={item} href={`#${item.toLowerCase().replace(" ", "")}`}
              onClick={() => setMenuOpen(false)}
              style={{ color: colors.secondary, fontWeight: 700, fontSize: 12, letterSpacing: "0.04em", textDecoration: "none", textTransform: "uppercase", padding: "10px 0", borderBottom: "1px solid rgba(195,198,215,0.15)" }}>
              {item}
            </a>
          ))}
          <button style={{ marginTop: 10, background: "none", border: `1px solid ${colors.primary}`, borderRadius: 7, padding: "9px 0", fontWeight: 700, fontSize: 12, color: colors.primary, cursor: "pointer", fontFamily: "'Montserrat', sans-serif" }}>
            Login
          </button>
        </div>
      )}
    </header>
  );
}

// ─── Hero ─────────────────────────────────────────────────────────────────────
function Hero() {
  const { isMobile, isTablet } = useBreakpoint();
  return (
    <section style={{
      paddingTop: isMobile ? 90 : 110,
      paddingBottom: isMobile ? 56 : 88,
      textAlign: "center",
      position: "relative",
      overflow: "hidden",
      backgroundImage: "radial-gradient(at 0% 0%, rgba(37,99,235,0.08) 0px, transparent 50%), radial-gradient(at 100% 0%, rgba(111,251,190,0.05) 0px, transparent 50%)",
      backgroundColor: colors.background,
      borderBottom: "1px solid rgba(195,198,215,0.2)",
      fontFamily: "'Montserrat', sans-serif",
    }}>
      {/* Constrained inner */}
      <div style={{ ...inner(isMobile) }}>
        <div style={{ maxWidth: 760, margin: "0 auto" }}>
          <div style={{ display: "inline-block", padding: "5px 12px", borderRadius: 9999, background: "rgba(0,74,198,0.05)", border: "1px solid rgba(0,74,198,0.1)", color: colors.primary, fontSize: 10, fontWeight: 700, letterSpacing: "0.1em", textTransform: "uppercase", marginBottom: 18 }}>
            A Complete Trading System
          </div>
          <h1 style={{ fontSize: isMobile ? 28 : isTablet ? 38 : 50, fontWeight: 800, lineHeight: 1.14, color: colors.onBackground, marginBottom: 16, letterSpacing: "-0.02em" }}>
            A Premium Trade Journal,<br />
            <span style={{ background: `linear-gradient(135deg, ${colors.primary}, ${colors.primaryContainer})`, WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>
              Without The Subscription.
            </span>
          </h1>
          <p style={{ fontSize: isMobile ? 14 : 15, color: colors.secondary, fontWeight: 500, maxWidth: 520, margin: "0 auto 14px", lineHeight: 1.65 }}>
            A complete execution database and performance analysis system for serious traders.
          </p>
          <div style={{ display: "flex", flexWrap: "wrap", justifyContent: "center", gap: 6, marginBottom: 28, alignItems: "center" }}>
            {["Log trades.", "Capture decisions.", "Track psychology."].map(t => (
              <span key={t} style={{ color: colors.primary, fontWeight: 700, fontSize: isMobile ? 13 : 14 }}>{t}</span>
            ))}
            <span style={{ color: colors.onBackground, fontWeight: 400, fontSize: isMobile ? 13 : 14 }}>
              Identify patterns. Refine execution. <strong style={{ fontWeight: 800 }}>Build your edge.</strong>
            </span>
          </div>
          <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 12 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap", justifyContent: "center" }}>
              <Stars size={15} />
              <span style={{ color: colors.secondary, fontSize: 11, fontWeight: 600 }}>
                Trusted by thousands of traders · See our reviews on Trustpilot
              </span>
            </div>
            <div style={{ padding: 4, borderRadius: 9999, backgroundImage: DASHED_BORDER, display: "inline-block" }}>
              <button style={{ background: colors.primary, color: "#fff", border: "none", borderRadius: 9999, padding: isMobile ? "12px 30px" : "14px 48px", fontWeight: 800, fontSize: 13, cursor: "pointer", display: "flex", alignItems: "center", gap: 8, boxShadow: "0 8px 32px rgba(0,74,198,0.3)", fontFamily: "'Montserrat', sans-serif", transition: "transform 0.2s" }}
                onMouseEnter={e => e.currentTarget.style.transform = "scale(1.04)"}
                onMouseLeave={e => e.currentTarget.style.transform = "scale(1)"}>
                Start Now - It's Free! →
              </button>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

// ─── Features ─────────────────────────────────────────────────────────────────
function Features() {
  const { isMobile } = useBreakpoint();
  const features = [
    { icon: "📅", title: "Stay Organised", desc: "Track your trades and review your performance by day, week, or month with our automated logging." },
    { icon: "📊", title: "Analyze Strategies", desc: "Easily analyze and compare the success rates of different strategies across varying market conditions." },
    { icon: "💎", title: "Spot Patterns", desc: "Identify patterns in your wins and losses to refine your trading schedule and avoid common pitfalls." },
    { icon: "📘", title: "Professional Journaling", desc: "Journal your trades and thoughts like a pro trader. Capture the psychology behind every execution." },
  ];
  return (
    <section id="features" style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile) }}>
        <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : "1fr 1fr", gap: 16 }}>
          {features.map(f => (
            <GlassCard key={f.title} style={{ padding: isMobile ? 18 : 28, borderRadius: 12, display: "flex", gap: 16, alignItems: "flex-start" }}>
              <div style={{ width: 42, height: 42, background: "rgba(0,74,198,0.08)", border: "1px solid rgba(0,74,198,0.15)", borderRadius: 8, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 18, flexShrink: 0 }}>
                {f.icon}
              </div>
              <div>
                <h3 style={{ fontSize: 15, fontWeight: 800, color: colors.onBackground, marginBottom: 5, marginTop: 0 }}>{f.title}</h3>
                <p style={{ color: colors.secondary, fontSize: 13, lineHeight: 1.6, margin: 0 }}>{f.desc}</p>
              </div>
            </GlassCard>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Performance Insights ─────────────────────────────────────────────────────
function PerformanceInsights() {
  const { isMobile } = useBreakpoint();
  const rows = [
    { inst: "EURUSD", phase: "London Mid", mom: "Strong", wl: "8 100%", wlGreen: true, momo: "Bearish", momoVal: "37 81%", tf: "1H", tfVal: "1 100%" },
    { inst: "EURUSD", phase: "London Open", mom: "Moderate", wl: "1 100%", wlGreen: true, momo: "Bullish", momoVal: "22 64%", tf: "1M", tfVal: "2 60%" },
    { inst: "EURUSD", phase: "New York Mid", mom: "Strong", wl: "12 83%", wlGreen: true, momo: "", momoVal: "", tf: "", tfVal: "" },
    { inst: "EURUSD", phase: "Sydney Open", mom: "Strong", wl: "2 50%", wlGreen: false, momo: "", momoVal: "", tf: "1HR", tfVal: "58 80%" },
  ];
  return (
    <section style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, background: "rgba(239,244,255,0.5)", borderTop: "1px solid rgba(195,198,215,0.2)", borderBottom: "1px solid rgba(195,198,215,0.2)", fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile) }}>
        <div style={{ display: "flex", alignItems: "center", gap: 14, marginBottom: 28, flexWrap: "wrap" }}>
          <div style={{ width: 46, height: 46, background: colors.primary, borderRadius: 10, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 20, boxShadow: "0 4px 20px rgba(0,74,198,0.2)", flexShrink: 0 }}>📈</div>
          <div>
            <h2 style={{ fontSize: isMobile ? 20 : 26, fontWeight: 800, color: colors.onBackground, margin: "0 0 3px" }}>Performance Insights</h2>
            <p style={{ color: colors.secondary, fontSize: 13, margin: 0 }}>Break down your win rate by instrument, session, market regime, strategy, and timeframe.</p>
          </div>
        </div>
        <GlassCard style={{ borderRadius: 12, overflow: "hidden" }} hover={false}>
          <div style={{ overflowX: "auto", WebkitOverflowScrolling: "touch" }}>
            <table style={{ width: "100%", borderCollapse: "collapse", minWidth: 640 }}>
              <thead>
                <tr style={{ background: "rgba(248,249,255,0.5)", borderBottom: "1px solid rgba(195,198,215,0.2)" }}>
                  {["Instrument", "Phase", "Momentum", "Win / Loss", "Momo", "TF"].map(h => (
                    <th key={h} style={{ padding: "10px 14px", textAlign: "left", fontSize: 10, fontWeight: 700, color: colors.secondary, textTransform: "uppercase", letterSpacing: "0.05em", whiteSpace: "nowrap" }}>{h}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {rows.map((r, i) => (
                  <tr key={i} style={{ borderBottom: "1px solid rgba(195,198,215,0.1)", transition: "background 0.15s" }}
                    onMouseEnter={e => e.currentTarget.style.background = "rgba(219,225,255,0.15)"}
                    onMouseLeave={e => e.currentTarget.style.background = "transparent"}>
                    <td style={{ padding: "12px 14px", fontWeight: 800, fontSize: 12, whiteSpace: "nowrap" }}>{r.inst}</td>
                    <td style={{ padding: "12px 14px", color: colors.secondary, fontSize: 12, whiteSpace: "nowrap" }}>{r.phase}</td>
                    <td style={{ padding: "12px 14px", color: colors.secondary, fontSize: 12 }}>{r.mom}</td>
                    <td style={{ padding: "12px 14px" }}>
                      <span style={{ background: r.wlGreen ? "rgba(0,98,66,0.08)" : "rgba(186,26,26,0.08)", color: r.wlGreen ? colors.tertiary : colors.error, padding: "3px 8px", borderRadius: 4, fontFamily: "'DM Mono', monospace", fontWeight: 700, fontSize: 12, whiteSpace: "nowrap" }}>{r.wl}</span>
                    </td>
                    <td style={{ padding: "12px 14px", fontSize: 12 }}>
                      {r.momo && (
                        <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
                          <span style={{ width: 6, height: 6, borderRadius: "50%", background: r.momo === "Bearish" ? colors.error : colors.tertiary, flexShrink: 0 }} />
                          <span style={{ color: colors.secondary }}>{r.momo}</span>
                          <Mono style={{ marginLeft: "auto", color: colors.tertiary, fontWeight: 700, fontSize: 12, paddingLeft: 8 }}>{r.momoVal}</Mono>
                        </div>
                      )}
                    </td>
                    <td style={{ padding: "12px 14px", fontSize: 12 }}>
                      {r.tf && (
                        <div style={{ display: "flex", justifyContent: "space-between", gap: 8, alignItems: "center" }}>
                          <Mono style={{ color: colors.secondary }}>{r.tf}</Mono>
                          <Mono style={{ color: colors.tertiary, fontWeight: 700 }}>{r.tfVal}</Mono>
                        </div>
                      )}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </GlassCard>
      </div>
    </section>
  );
}

// ─── Monthly Equity ───────────────────────────────────────────────────────────
function MonthlyEquity() {
  const { isMobile, isTablet } = useBreakpoint();
  const cols = isMobile ? 2 : isTablet ? 3 : 6;
  const months = [
    { label: "JAN/2024", val: "+12.48%", green: true, rec: "100% rec.", cause: "Mixed", maxDD: "0.00%", bigL: "0.00%", loss: "0/3" },
    { label: "FEB/2024", val: "+8.24%", green: true, rec: "100% rec.", cause: "Mixed", maxDD: "0.00%", bigL: "0.00%", loss: "0/2" },
    { label: "MAR/2024", val: "+13.40%", green: true, rec: "100% rec.", cause: "Mixed", maxDD: "-2.00%", bigL: "-2.00%", loss: "2/5" },
    { label: "SEP/2024", val: "-2.00%", green: false, rec: "0% rec.", cause: "Carrying -2.00%", maxDD: "-2.00%", bigL: "-2.00%", loss: "1/1" },
    { label: "NOV/2024", val: "-2.47%", green: false, rec: "0% rec.", cause: "Carrying -2.47%", maxDD: "-4.00%", bigL: "-4.00%", loss: "3/5" },
    { label: "JAN/2025", val: "+10.99%", green: true, rec: "100% rec.", cause: "Mixed", maxDD: "0.00%", bigL: "0.00%", loss: "0/2" },
  ];
  return (
    <section style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, fontFamily: "'Montserrat', sans-serif", borderBottom: "1px solid rgba(195,198,215,0.2)" }}>
      <div style={{ ...inner(isMobile) }}>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end", flexWrap: "wrap", gap: 8, borderBottom: "1px solid rgba(195,198,215,0.3)", paddingBottom: 14, marginBottom: 24 }}>
          <div>
            <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: "0.08em", textTransform: "uppercase", color: colors.secondary, display: "block", marginBottom: 3 }}>MONTHLY EQUITY GROWTH • 2024–2025</span>
            <h2 style={{ fontSize: isMobile ? 20 : 26, fontWeight: 800, color: colors.onBackground, margin: 0 }}>Monthly Equity Growth</h2>
          </div>
          {!isMobile && <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: "0.08em", textTransform: "uppercase", color: colors.secondary }}>DOMINANT CAUSE PER MONTH</span>}
        </div>
        <div style={{ display: "grid", gridTemplateColumns: `repeat(${cols}, 1fr)`, gap: 12 }}>
          {months.map(m => (
            <div key={m.label} style={{ border: "1px solid rgba(195,198,215,0.3)", borderRadius: 8, padding: 12, textAlign: "center", display: "flex", flexDirection: "column", alignItems: "center", background: "#fff", boxShadow: "0 1px 4px rgba(0,0,0,0.04)" }}>
              <div style={{ width: 6, height: 6, borderRadius: "50%", background: m.green ? colors.neonGreen : colors.error, marginBottom: 7 }} />
              <div style={{ fontSize: 9, fontWeight: 800, textTransform: "uppercase", marginBottom: 3, letterSpacing: "0.04em", color: colors.secondary }}>{m.label}</div>
              <Mono style={{ fontSize: 16, fontWeight: 700, color: m.green ? colors.tertiary : colors.error, display: "block", marginBottom: 2 }}>{m.val}</Mono>
              <div style={{ fontSize: 9, fontWeight: 700, marginBottom: 8, color: colors.secondary }}>{m.rec}</div>
              <div style={{ width: "100%", borderRadius: 4, padding: "3px 0", marginBottom: 8, fontSize: 9, fontWeight: 700, textTransform: "uppercase", color: m.green ? "#f97316" : colors.error, background: m.green ? "transparent" : "rgba(186,26,26,0.08)" }}>{m.cause}</div>
              <div style={{ width: "100%", fontSize: 9 }}>
                {[["MAX DD", m.maxDD], ["BIG L", m.bigL], ["LOSS", m.loss]].map(([k, v]) => (
                  <div key={k} style={{ display: "flex", justifyContent: "space-between", marginBottom: 2 }}>
                    <Mono style={{ color: colors.secondary }}>{k}</Mono>
                    <Mono style={{ color: colors.onBackground, fontWeight: 600 }}>{v}</Mono>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Trading Calendar ─────────────────────────────────────────────────────────
function TradingCalendar() {
  const { isMobile } = useBreakpoint();
  const days = Array.from({ length: 35 }, (_, i) => {
    const d = i - 3;
    if (d <= 0 || d > 31) return { label: d <= 0 ? 26 + i : d - 31, faded: true };
    if (d === 6) return { label: 6, profit: "+$358", trades: "1T·100%W", active: true };
    return { label: d, faded: false };
  });
  const stats = [
    { label: "NET P&L", val: "+$2923.81", sub: "3W / 0L", green: true },
    { label: "WIN RATE", val: "100%", sub: "3 profit days", green: true },
    { label: "TRADES", val: "7", sub: "All active days", green: true },
    { label: "W/L RATIO", val: "—", sub: "Avg win ÷ avg loss", green: false },
  ];
  return (
    <section style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, background: colors.surface, fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile) }}>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", borderBottom: "1px solid rgba(195,198,215,0.2)", paddingBottom: 14, marginBottom: 0, flexWrap: "wrap", gap: 10 }}>
          <div>
            <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: "0.08em", color: colors.secondary, display: "block", marginBottom: 3, textTransform: "uppercase" }}>PERFORMANCE OVERVIEW</span>
            <h2 style={{ fontFamily: "'DM Mono', monospace", fontSize: isMobile ? 14 : 17, fontWeight: 700, color: colors.onBackground, margin: 0, letterSpacing: "0.04em" }}>TRADING_CALENDAR</h2>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 4, background: "rgba(255,255,255,0.7)", border: "1px solid rgba(195,198,215,0.3)", borderRadius: 7, padding: "3px 4px" }}>
            <button style={{ padding: "2px 6px", background: "none", border: "none", cursor: "pointer", fontSize: 15, color: colors.onBackground, lineHeight: 1 }}>‹</button>
            <span style={{ fontWeight: 700, fontSize: 11, textTransform: "uppercase" }}>MAY</span>
            <Mono style={{ fontWeight: 700, fontSize: 11 }}>2026</Mono>
            <button style={{ padding: "2px 6px", background: "none", border: "none", cursor: "pointer", fontSize: 15, color: colors.onBackground, lineHeight: 1 }}>›</button>
          </div>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr 1fr" : "repeat(4, 1fr)", background: "rgba(255,255,255,0.7)", backdropFilter: "blur(12px)", border: "1px solid rgba(226,232,240,0.8)", borderBottom: "none", borderRadius: "10px 10px 0 0", overflow: "hidden" }}>
          {stats.map((s, i) => (
            <div key={s.label} style={{ padding: isMobile ? 12 : 16, borderRight: isMobile ? (i % 2 === 0 ? "1px solid rgba(195,198,215,0.3)" : "none") : (i < 3 ? "1px solid rgba(195,198,215,0.3)" : "none"), borderBottom: isMobile && i < 2 ? "1px solid rgba(195,198,215,0.3)" : "none" }}>
              <div style={{ fontSize: 9, fontWeight: 700, color: colors.secondary, textTransform: "uppercase", letterSpacing: "0.05em", marginBottom: 4 }}>{s.label}</div>
              <Mono style={{ fontSize: isMobile ? 16 : 20, fontWeight: 700, color: s.green ? colors.tertiary : colors.onBackground, display: "block", lineHeight: 1.2 }}>{s.val}</Mono>
              <div style={{ fontSize: 10, fontWeight: 600, color: colors.secondary, marginTop: 2 }}>{s.sub}</div>
            </div>
          ))}
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(7, 1fr)", border: "1px solid rgba(226,232,240,0.8)", borderRadius: "0 0 10px 10px", background: "rgba(255,255,255,0.7)", backdropFilter: "blur(12px)", overflow: "hidden" }}>
          {["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"].map((d, i) => (
            <div key={d} style={{ padding: isMobile ? "5px 4px" : "7px 8px", borderBottom: "1px solid rgba(195,198,215,0.2)", borderRight: i < 6 ? "1px solid rgba(195,198,215,0.2)" : "none", textAlign: "center", fontSize: 9, fontWeight: 700, color: colors.secondary, fontFamily: "'DM Mono', monospace", background: colors.surfaceContainerLow }}>{d}</div>
          ))}
          {days.map((d, i) => (
            <div key={i} style={{ padding: isMobile ? "5px 4px" : "7px 8px", minHeight: isMobile ? 44 : 64, borderBottom: "1px solid rgba(195,198,215,0.15)", borderRight: (i + 1) % 7 !== 0 ? "1px solid rgba(195,198,215,0.15)" : "none", background: d.active ? "rgba(0,255,136,0.04)" : d.faded ? colors.surfaceContainerLow : "transparent", display: "flex", flexDirection: "column", justifyContent: "space-between" }}>
              <span style={{ fontSize: 9, fontFamily: "'DM Mono', monospace", color: d.active ? colors.neonGreen : colors.secondary, fontWeight: 700 }}>{d.label}</span>
              {d.profit && (
                <div>
                  <div style={{ color: colors.neonGreen, fontWeight: 700, fontFamily: "'DM Mono', monospace", fontSize: isMobile ? 9 : 11 }}>{d.profit}</div>
                  <div style={{ fontSize: 8, color: colors.secondary, fontFamily: "'DM Mono', monospace" }}>{d.trades}</div>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Mobile Mockup ────────────────────────────────────────────────────────────
function MobileMockup() {
  const { isMobile, isTablet } = useBreakpoint();
  return (
    <section style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, background: colors.surface, borderTop: "1px solid rgba(195,198,215,0.2)", fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile), display: "flex", alignItems: "center", gap: isMobile ? 0 : 56, flexDirection: isMobile ? "column" : "row" }}>
        <div style={{ flexShrink: 0, display: "flex", justifyContent: "center", width: isMobile ? "100%" : "auto", marginBottom: isMobile ? 36 : 0 }}>
          <div style={{ position: "relative", width: 230, height: 460, background: "#fff", borderRadius: "2.6rem", boxShadow: "0 30px 80px rgba(0,0,0,0.13)", padding: 13, border: "6px solid #1e293b", outline: "3px solid rgba(30,41,59,0.12)" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "5px 5px 7px", borderBottom: "1px solid #f1f5f9", marginBottom: 8 }}>
              <Logo size={9} />
              <span style={{ fontSize: 13 }}>📈</span>
            </div>
            <div style={{ display: "flex", flexDirection: "column", gap: 9, padding: "0 2px" }}>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 6 }}>
                {[{ val: "67.92%", label: "Winrate", pct: 67 }, { val: "97.46%", label: "Daily W/R", pct: 97 }].map(s => (
                  <div key={s.label} style={{ background: colors.surfaceContainerLow, padding: 8, borderRadius: 9, border: "1px solid rgba(195,198,215,0.3)" }}>
                    <Mono style={{ fontSize: 12, color: colors.primary, fontWeight: 500, display: "block" }}>{s.val}</Mono>
                    <div style={{ fontSize: 8, color: colors.secondary, fontWeight: 700, marginBottom: 5 }}>{s.label}</div>
                    <div style={{ height: 3, background: "#e2e8f0", borderRadius: 9999, overflow: "hidden" }}><div style={{ height: "100%", width: `${s.pct}%`, background: colors.primary }} /></div>
                  </div>
                ))}
              </div>
              <div style={{ background: colors.surfaceContainerLow, padding: 8, borderRadius: 9 }}>
                <Mono style={{ fontSize: 14, color: colors.primary, fontWeight: 500, display: "block" }}>$19,800.72</Mono>
                <div style={{ fontSize: 8, color: colors.secondary, fontWeight: 700, marginBottom: 5 }}>Total Profit</div>
                <svg width="100%" height="40" viewBox="0 0 200 40" fill="none" stroke={colors.primary} strokeWidth="2"><path d="M0 36 Q50 26 100 32 T200 4" /></svg>
              </div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 6 }}>
                {[{ val: "1,131", label: "Trades" }, { val: "0.89", label: "Avg W/L" }].map(s => (
                  <div key={s.label} style={{ background: colors.surfaceContainerLow, padding: 8, borderRadius: 9, border: "1px solid rgba(195,198,215,0.3)" }}>
                    <Mono style={{ fontSize: 12, color: colors.onBackground, fontWeight: 500, display: "block" }}>{s.val}</Mono>
                    <div style={{ fontSize: 8, color: colors.secondary, fontWeight: 700 }}>{s.label}</div>
                  </div>
                ))}
              </div>
            </div>
            <div style={{ position: "absolute", bottom: 12, left: "50%", transform: "translateX(-50%)", width: 50, height: 4, background: "#e2e8f0", borderRadius: 9999 }} />
          </div>
        </div>
        <div style={{ flex: 1, textAlign: isMobile ? "center" : "left" }}>
          <h2 style={{ fontSize: isMobile ? 22 : isTablet ? 26 : 32, fontWeight: 800, color: colors.onBackground, lineHeight: 1.22, marginBottom: 12, letterSpacing: "-0.02em" }}>
            Realtime Statistics Keep You Updated Anywhere In The World
          </h2>
          <p style={{ fontSize: 14, color: colors.secondary, marginBottom: 24, lineHeight: 1.65 }}>Automatic import from your MT5 account makes tracking your performance easier than ever.</p>
          <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: 12, alignItems: isMobile ? "center" : "flex-start" }}>
            {["Fully automated process", "Mobile friendly", "Check your performance in realtime", "Lightweight and optimised"].map(item => (
              <li key={item} style={{ display: "flex", alignItems: "center", gap: 12, fontWeight: 700, color: colors.onBackground, fontSize: 13 }}>
                <span style={{ width: 32, height: 32, background: "rgba(0,74,198,0.08)", border: "1px solid rgba(0,74,198,0.15)", borderRadius: 7, display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0, fontSize: 13 }}>✓</span>
                {item}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>
  );
}

// ─── Pricing ──────────────────────────────────────────────────────────────────
function Pricing() {
  const { isMobile, isTablet } = useBreakpoint();
  const cols = isMobile ? 1 : isTablet ? 2 : 4;
  const plans = [
    { name: "Free", price: "$0", period: "/forever", desc: "Core stats to get you started.", features: ["Total P&L & Win Rate", "R Expectancy & Profit Factor", "Avg Trade & Trade Count", "Trade calendar view", "Link MT4 & MT5"] },
    { name: "Weekly", price: "$7", period: "/week", desc: "Try the full journal risk-free.", features: ["Everything in Free", "Full journal access", "Detailed analytics & drawdown", "Strategy audit engine", "Edge building tools"] },
    { name: "Monthly", price: "$20", period: "/month", desc: "Full power for serious traders.", features: ["Everything in Weekly", "AI Coach (Trader AI Chat)", "AI trade validation", "Behavior pattern breakdown"], highlight: true, badge: "Most Popular", addon: "+ TradeSync Copier → $27/mo" },
    { name: "Yearly", price: "$180", period: "/year", desc: "Best value for committed traders.", savings: "Save $60 vs monthly · $15/mo", features: ["Everything in Monthly", "Annual performance review", "Dedicated onboarding"], badge: "Best Value", addon: "+ TradeSync Copier → $220/yr" },
  ];
  return (
    <section id="pricing" style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, background: colors.surface, fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile) }}>
        <div style={{ textAlign: "center", marginBottom: isMobile ? 36 : 52 }}>
          <h2 style={{ fontSize: isMobile ? 24 : 36, fontWeight: 800, color: colors.onBackground, marginBottom: 8 }}>Simple, Transparent Pricing</h2>
          <p style={{ color: colors.secondary, fontSize: 14 }}>Start free. Upgrade when you're ready.</p>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: `repeat(${cols}, 1fr)`, gap: isMobile ? 16 : 20, alignItems: "start" }}>
          {plans.map(plan => (
            <div key={plan.name} style={{ position: "relative", background: "rgba(255,255,255,0.7)", backdropFilter: "blur(12px)", border: plan.highlight ? `2px solid ${colors.primary}` : "1px solid rgba(226,232,240,0.8)", borderRadius: 12, padding: 22, display: "flex", flexDirection: "column", transform: !isMobile && plan.highlight ? "scale(1.03)" : "none", zIndex: plan.highlight ? 2 : 1, boxShadow: plan.highlight ? "0 20px 60px rgba(0,74,198,0.12)" : "0 4px 20px rgba(0,0,0,0.04)" }}>
              {plan.badge && (
                <div style={{ position: "absolute", top: -12, left: "50%", transform: "translateX(-50%)", background: plan.highlight ? colors.primary : colors.tertiary, color: "#fff", padding: "3px 12px", borderRadius: 9999, fontSize: 10, fontWeight: 800, textTransform: "uppercase", whiteSpace: "nowrap", boxShadow: "0 4px 12px rgba(0,0,0,0.12)" }}>{plan.badge}</div>
              )}
              <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: "0.08em", color: colors.secondary, textTransform: "uppercase", marginBottom: 8, display: "block" }}>{plan.name}</span>
              <div style={{ marginBottom: 8, display: "flex", alignItems: "baseline", gap: 4 }}>
                <Mono style={{ fontSize: isMobile ? 34 : 38, fontWeight: 800, color: colors.onBackground, lineHeight: 1 }}>{plan.price}</Mono>
                <span style={{ color: colors.secondary, fontSize: 11, fontWeight: 700 }}>{plan.period}</span>
              </div>
              {plan.savings && <p style={{ color: colors.tertiary, fontWeight: 800, fontSize: 11, marginBottom: 8, marginTop: 0 }}>{plan.savings}</p>}
              <p style={{ color: colors.secondary, fontSize: 13, marginBottom: 12, marginTop: 0, lineHeight: 1.5 }}>{plan.desc}</p>
              {plan.addon && (
                <div style={{ background: plan.highlight ? "rgba(0,74,198,0.05)" : "rgba(0,98,66,0.05)", border: `1px solid ${plan.highlight ? "rgba(0,74,198,0.2)" : "rgba(0,98,66,0.2)"}`, borderRadius: 7, padding: "6px 10px", marginBottom: 14, fontSize: 10, fontWeight: 800, color: plan.highlight ? colors.primary : colors.tertiary, textAlign: "center" }}><Mono>{plan.addon}</Mono></div>
              )}
              <div style={{ height: 1, background: "rgba(195,198,215,0.3)", marginBottom: 14 }} />
              <ul style={{ listStyle: "none", padding: 0, margin: "0 0 18px", flex: 1, display: "flex", flexDirection: "column", gap: 8 }}>
                {plan.features.map(f => (
                  <li key={f} style={{ display: "flex", gap: 8, alignItems: "flex-start", fontSize: 13, color: colors.secondary, lineHeight: 1.4 }}>
                    <span style={{ color: plan.highlight ? colors.primary : colors.tertiary, fontWeight: 700, fontSize: 14, lineHeight: 1.3, flexShrink: 0 }}>✓</span>{f}
                  </li>
                ))}
              </ul>
              <button style={{ background: plan.highlight ? colors.primary : "transparent", color: plan.highlight ? "#fff" : colors.primary, border: `2px solid ${colors.primary}`, borderRadius: 8, padding: "9px 0", fontWeight: 800, fontSize: 12, cursor: "pointer", fontFamily: "'Montserrat', sans-serif", transition: "all 0.2s" }}
                onMouseEnter={e => { e.currentTarget.style.background = colors.primary; e.currentTarget.style.color = "#fff"; }}
                onMouseLeave={e => { e.currentTarget.style.background = plan.highlight ? colors.primary : "transparent"; e.currentTarget.style.color = plan.highlight ? "#fff" : colors.primary; }}>
                Get Started
              </button>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Reviews ──────────────────────────────────────────────────────────────────
function Reviews() {
  const { isMobile, isTablet } = useBreakpoint();
  const cols = isMobile ? 1 : isTablet ? 2 : 4;
  const reviews = [
    { text: "The dashboard is incredibly customizable and very convenient to use.", name: "Alex M." },
    { text: "I love how Myfmjournal helps me track my performance and improve my strategies.", name: "Jordan K." },
    { text: "This tool is fantastic, the user interface could be even more intuitive.", name: "Sarah T." },
    { text: "Myfmjournal has changed the way I analyze my trading operations.", name: "Michael R." },
  ];
  return (
    <section id="reviews" style={{ paddingTop: isMobile ? 48 : 72, paddingBottom: isMobile ? 48 : 72, background: "rgba(239,244,255,0.3)", borderTop: "1px solid rgba(195,198,215,0.2)", fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile) }}>
        <h2 style={{ textAlign: "center", fontSize: isMobile ? 22 : 30, fontWeight: 800, color: colors.onBackground, marginBottom: isMobile ? 28 : 44 }}>
          Join <Mono>10,000+</Mono> Traders Who Chose Myfmjournal
        </h2>
        <div style={{ display: "grid", gridTemplateColumns: `repeat(${cols}, 1fr)`, gap: 16 }}>
          {reviews.map(r => (
            <GlassCard key={r.name} style={{ padding: 20, borderRadius: 12, display: "flex", flexDirection: "column", gap: 10 }}>
              <Stars size={14} />
              <p style={{ color: colors.onBackground, fontSize: 13, lineHeight: 1.7, fontStyle: "italic", flex: 1, margin: 0 }}>"{r.text}"</p>
              <div style={{ borderTop: "1px solid rgba(195,198,215,0.2)", paddingTop: 10, fontWeight: 800, color: colors.onBackground, fontSize: 12 }}>{r.name}</div>
            </GlassCard>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Final CTA ────────────────────────────────────────────────────────────────
function FinalCTA() {
  const { isMobile } = useBreakpoint();
  return (
    <section style={{ paddingTop: isMobile ? 56 : 80, paddingBottom: isMobile ? 56 : 80, textAlign: "center", background: colors.surface, position: "relative", overflow: "hidden", fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ position: "absolute", inset: 0, backgroundImage: "radial-gradient(at 50% 0%, rgba(37,99,235,0.06) 0px, transparent 60%)", pointerEvents: "none" }} />
      <div style={{ ...inner(isMobile), position: "relative", zIndex: 1 }}>
        <h2 style={{ fontSize: isMobile ? 24 : 36, fontWeight: 800, color: colors.onBackground, marginBottom: 22 }}>Ready to Refine Your Edge?</h2>
        <div style={{ padding: 4, borderRadius: 9999, backgroundImage: DASHED_BORDER, display: "inline-block" }}>
          <button style={{ background: colors.primary, color: "#fff", border: "none", borderRadius: 9999, padding: isMobile ? "13px 34px" : "14px 48px", fontWeight: 800, fontSize: 13, cursor: "pointer", display: "flex", alignItems: "center", gap: 8, boxShadow: "0 8px 32px rgba(0,74,198,0.28)", fontFamily: "'Montserrat', sans-serif", transition: "transform 0.2s" }}
            onMouseEnter={e => e.currentTarget.style.transform = "scale(1.04)"}
            onMouseLeave={e => e.currentTarget.style.transform = "scale(1)"}>
            Join us now →
          </button>
        </div>
        <div style={{ marginTop: 12, color: colors.secondary, fontSize: 12, fontWeight: 600 }}>Start your free trial today.</div>
      </div>
    </section>
  );
}

// ─── Footer ───────────────────────────────────────────────────────────────────
function Footer() {
  const { isMobile, isTablet } = useBreakpoint();
  return (
    <footer style={{ background: "#fff", borderTop: "1px solid rgba(195,198,215,0.3)", fontFamily: "'Montserrat', sans-serif" }}>
      <div style={{ ...inner(isMobile), paddingTop: isMobile ? 36 : 52, paddingBottom: isMobile ? 36 : 52 }}>
        <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : isTablet ? "1fr 1fr" : "2fr 1fr 1fr 2fr", gap: isMobile ? 28 : 36, alignItems: "start" }}>
          <div>
            <div style={{ marginBottom: 10 }}><Logo size={20} /></div>
            <p style={{ color: colors.secondary, fontSize: 13, lineHeight: 1.65, margin: 0 }}>A professional-grade trading journal and analytics platform built for serious traders.</p>
          </div>
          <div>
            <h4 style={{ fontWeight: 800, color: colors.onBackground, marginBottom: 14, marginTop: 0, fontSize: 13 }}>Community</h4>
            <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
              {["Twitter", "YouTube", "Facebook"].map(l => (
                <a key={l} href="#" style={{ color: colors.secondary, fontWeight: 500, fontSize: 13, textDecoration: "none" }}
                  onMouseEnter={e => e.target.style.color = colors.primary}
                  onMouseLeave={e => e.target.style.color = colors.secondary}>{l}</a>
              ))}
            </div>
          </div>
          <div>
            <h4 style={{ fontWeight: 800, color: colors.onBackground, marginBottom: 14, marginTop: 0, fontSize: 13 }}>Tools</h4>
            <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
              {["Free Trading Journal", "Sessions", "Economic Calendar"].map(l => (
                <a key={l} href="#" style={{ color: colors.secondary, fontWeight: 500, fontSize: 13, textDecoration: "none" }}
                  onMouseEnter={e => e.target.style.color = colors.primary}
                  onMouseLeave={e => e.target.style.color = colors.secondary}>{l}</a>
              ))}
            </div>
          </div>
          <div>
            <h4 style={{ fontWeight: 800, color: colors.onBackground, marginBottom: 14, marginTop: 0, fontSize: 13 }}>Newsletter</h4>
            <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
              <input type="email" placeholder="your@email.com" style={{ background: colors.surface, border: "1px solid rgba(195,198,215,0.5)", borderRadius: 7, padding: "9px 13px", fontSize: 13, fontFamily: "'Montserrat', sans-serif", outline: "none", width: "100%", boxSizing: "border-box" }} />
              <button style={{ background: colors.primary, color: "#fff", border: "none", borderRadius: 7, padding: "9px 0", fontWeight: 800, fontSize: 12, cursor: "pointer", fontFamily: "'Montserrat', sans-serif" }}>Subscribe</button>
            </div>
          </div>
        </div>
      </div>
      <div style={{ borderTop: "1px solid rgba(195,198,215,0.2)" }}>
        <div style={{ ...inner(isMobile), paddingTop: 14, paddingBottom: 14, display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 10 }}>
          <span style={{ color: colors.secondary, fontSize: 11, fontWeight: 600 }}>© <Mono>2026</Mono> Myfmjournal — All rights reserved</span>
          <div style={{ display: "flex", gap: 18 }}>
            {["Privacy Policy", "Support", "Terms"].map(l => (
              <a key={l} href="#" style={{ color: colors.secondary, fontSize: 11, fontWeight: 600, textDecoration: "none" }}>{l}</a>
            ))}
          </div>
        </div>
      </div>
    </footer>
  );
}

// ─── App ──────────────────────────────────────────────────────────────────────
export default function App() {
  return (
    <>
      <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet" />
      <style>{`*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; } body { background: #f8f9ff; }`}</style>
      <div style={{ fontFamily: "'Montserrat', sans-serif", background: colors.background }}>
        <Navbar />
        <div style={{ paddingTop: 60 }}>
          <Hero />
          <Features />
          <PerformanceInsights />
          <MonthlyEquity />
          <TradingCalendar />
          <MobileMockup />
          <Pricing />
          <Reviews />
          <FinalCTA />
          <Footer />
        </div>
      </div>
    </>
  );
}
