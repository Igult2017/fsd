import React, { useState, useMemo } from 'react';
import { 
  TrendingUp, 
  AlertTriangle, 
  Brain, 
  Database, 
  Activity, 
  Target, 
  ShieldCheck, 
  Zap, 
  ChevronDown, 
  ChevronUp, 
  BarChart3,
  Search,
  PlusCircle,
  Clock,
  Settings,
  Cpu
} from 'lucide-react';

const App = () => {
  const [activeLevel, setActiveLevel] = useState(1);

  // Mock trade data - this would be dynamically generated from actual trades
  const tradeData = useMemo(() => ({
    instruments: ['EURUSD', 'GBPUSD', 'XAUUSD', 'USDJPY', 'AUDUSD', 'NZDUSD', 'USDCAD'],
    winFactors: ['ATR 1.2-1.8', 'LDN Open', 'RSI 40-60', 'Vol >2.0x', 'H1 Flip'],
    lossFactors: ['ATR <0.5', 'Spread >2.5', 'News <15m', 'Consolidation', 'Low Liq'],
    winCorrelations: {
      'EURUSD': [85, 72, 91, 68, 78],
      'GBPUSD': [62, 88, 54, 75, 81],
      'XAUUSD': [93, 45, 67, 89, 71],
      'USDJPY': [71, 69, 82, 61, 66],
      'AUDUSD': [58, 81, 73, 70, 59],
      'NZDUSD': [64, 76, 68, 64, 72],
      'USDCAD': [77, 58, 79, 82, 68]
    },
    lossCorrelations: {
      'EURUSD': [72, 89, 48, 81, 65],
      'GBPUSD': [68, 92, 52, 76, 71],
      'XAUUSD': [45, 67, 38, 88, 79],
      'USDJPY': [81, 78, 61, 72, 58],
      'AUDUSD': [76, 85, 55, 69, 82],
      'NZDUSD': [69, 81, 49, 74, 77],
      'USDCAD': [73, 74, 44, 79, 68]
    }
  }), []);

  // Navigation levels mapping
  const levels = [
    { id: 1, label: 'STRATEGY AUDIT', icon: <Search className="w-4 h-4" /> },
    { id: 2, label: 'EVIDENCE & PROOF', icon: <Database className="w-4 h-4" /> },
    { id: 3, label: 'DIAGNOSTICS', icon: <Activity className="w-4 h-4" /> },
    { id: 4, label: 'ACTION & ITERATION', icon: <PlusCircle className="w-4 h-4" /> },
  ];

  return (
    <div className="min-h-screen bg-slate-950 text-slate-100 font-sans selection:bg-blue-500/30">
      {/* Top Navigation Bar */}
      <nav className="border-b border-slate-800 bg-slate-900/50 backdrop-blur-md sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-3 sm:px-6 h-12 sm:h-16 flex items-center justify-between">
          {/* Logo & Title */}
          <div className="flex items-center gap-2 sm:gap-3 min-w-0 flex-shrink">
            <div className="w-6 h-6 sm:w-8 sm:h-8 bg-blue-600 rounded flex items-center justify-center shrink-0">
              <Cpu className="w-3.5 h-3.5 sm:w-5 sm:h-5 text-white" />
            </div>
            <div className="flex flex-col leading-tight min-w-0">
              <span className="font-bold text-[11px] sm:text-lg uppercase text-slate-100 whitespace-nowrap">
                <span className="hidden sm:inline tracking-[0.15em]">AI Strategy</span>
                <span className="sm:hidden tracking-tight">AI</span>
                <span className="text-blue-500">
                  <span className="hidden sm:inline tracking-[0.15em]"> Auditor</span>
                  <span className="sm:hidden tracking-tight"> Audit</span>
                </span>
              </span>
              <span className="hidden sm:block text-[9px] text-slate-500 font-bold tracking-[0.3em] uppercase">Institutional Grade</span>
            </div>
          </div>
          
          {/* Navigation - Desktop shows full nav, Mobile shows compact level selector */}
          <div className="flex items-center gap-3 sm:gap-6 shrink-0">
            {/* Desktop Navigation */}
            <div className="hidden md:flex bg-slate-800/50 p-1 rounded-lg border border-slate-700">
              {levels.map((level) => (
                <button
                  key={level.id}
                  onClick={() => setActiveLevel(level.id)}
                  className={`px-4 py-1.5 rounded-md text-xs font-semibold transition-all duration-200 flex items-center gap-2 ${
                    activeLevel === level.id 
                    ? 'bg-blue-600 text-white shadow-lg shadow-blue-900/20' 
                    : 'text-slate-400 hover:text-slate-100'
                  }`}
                >
                  {level.icon}
                  <span className="hidden lg:inline">{level.label}</span>
                  <span className="lg:hidden">L{level.id}</span>
                </button>
              ))}
            </div>

            {/* Mobile Navigation - Show in header */}
            <div className="md:hidden flex bg-slate-800/50 p-0.5 rounded-lg border border-slate-700">
              {levels.map((level) => (
                <button
                  key={level.id}
                  onClick={() => setActiveLevel(level.id)}
                  className={`px-2 py-1.5 rounded-md text-xs font-semibold transition-all duration-200 flex items-center gap-1 ${
                    activeLevel === level.id 
                    ? 'bg-blue-600 text-white' 
                    : 'text-slate-400'
                  }`}
                >
                  <span className="text-[10px]">L{level.id}</span>
                </button>
              ))}
            </div>
            
            {/* Status & Settings */}
            <div className="hidden sm:flex items-center gap-2 sm:gap-3 pl-2 sm:pl-4 border-l border-slate-800">
              <div className="hidden lg:flex flex-col items-end">
                <span className="text-[10px] text-slate-500 font-bold uppercase">Status</span>
                <span className="text-[10px] text-emerald-400 font-bold uppercase flex items-center gap-1">
                  <span className="w-1.5 h-1.5 bg-emerald-500 rounded-full animate-pulse"></span>
                  Active
                </span>
              </div>
              <Settings className="w-4 h-4 sm:w-5 sm:h-5 text-slate-400 cursor-pointer hover:text-white" />
            </div>
          </div>
        </div>
      </nav>

      <main className="max-w-7xl mx-auto px-6 py-8 pb-8">
        {/* Header Summary Stats */}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
          <StatCard label="Audit Win Rate" value="64.2%" trend="+2.1%" color="text-emerald-400" />
          <StatCard label="Edge Persistence" value="2.14" trend="+0.12" color="text-blue-400" />
          <StatCard label="Risk Entropy" value="Low" trend="-0.2" color="text-slate-400" />
          <StatCard label="AI Confidence" value="High" trend="92/100" color="text-purple-400" />
        </div>

        {/* Level 1: Strategy Audit */}
        {activeLevel === 1 && (
          <div className="space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
            <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-4">
              <h2 className="text-2xl font-bold flex items-center gap-3">
                <Search className="text-blue-500 shrink-0" /> LEVEL 1 — STRATEGY AUDIT
              </h2>
              <span className="text-xs bg-slate-800 px-3 py-1 rounded-full text-slate-400 border border-slate-700 w-fit">Audit Cycle: Q4 2023 - PRESENT</span>
            </div>

            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 items-start">
              {/* Executive Summary & Edge */}
              <div className="lg:col-span-2 space-y-6">
                <Section title="Executive Summary" icon={<Zap className="w-4 h-4 text-yellow-400" />}>
                  <div className="max-h-[120px] overflow-y-auto pr-2 custom-scrollbar">
                    <p className="text-slate-300 leading-relaxed text-sm">
                      The AI Auditor has identified a robust statistical edge during high-volatility London/NY overlap sessions. 
                      Recent performance indicates a tightening of the equity curve with a specific reduction in "fat tail" risk events.
                      System suggests shifting sizing from linear to volatility-weighted based on VIX thresholds above 18.
                    </p>
                  </div>
                </Section>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                  <Section title="Is There an Edge?" icon={<ShieldCheck className="w-4 h-4 text-emerald-400" />}>
                    <div className="flex items-center gap-4 mb-2">
                      <div className="text-3xl font-bold text-emerald-400">YES</div>
                      <div className="h-2 flex-1 bg-slate-800 rounded-full overflow-hidden">
                        <div className="h-full bg-emerald-500" style={{ width: '88%' }}></div>
                      </div>
                    </div>
                    <p className="text-xs text-slate-400 font-mono tracking-tighter truncate">Confidence Interval: 95% (+/- 2.1%) based on 482 samples.</p>
                  </Section>

                  <Section title="Edge Drivers" icon={<TrendingUp className="w-4 h-4 text-blue-400" />}>
                    <div className="max-h-[80px] overflow-y-auto pr-2 custom-scrollbar">
                      <ul className="text-sm space-y-2">
                        <li className="flex items-start gap-2">
                          <div className="w-1.5 h-1.5 rounded-full bg-blue-500 mt-1.5 shrink-0" />
                          <span className="truncate lg:whitespace-normal">Mean Reversion during Asian session lulls</span>
                        </li>
                        <li className="flex items-start gap-2">
                          <div className="w-1.5 h-1.5 rounded-full bg-blue-500 mt-1.5 shrink-0" />
                          <span className="truncate lg:whitespace-normal">Momentum continuation at H1 supply/demand flips</span>
                        </li>
                      </ul>
                    </div>
                  </Section>
                </div>
              </div>

              {/* Sidebar: Monitor & Changes */}
              <div className="space-y-6">
                <Section title="What to Monitor Next" icon={<Activity className="w-4 h-4 text-orange-400" />}>
                  <div className="max-h-[120px] overflow-y-auto pr-2 space-y-3 custom-scrollbar">
                    <MonitorItem label="Correlated USD Strength" status="High Priority" color="border-orange-500/50" />
                    <MonitorItem label="H4 Liquidity Gaps" status="Monitoring" color="border-slate-700" />
                    <MonitorItem label="Volatility Contraction" status="New" color="border-blue-500/50" />
                  </div>
                </Section>
                
                <Section title="Audit-Driven Changes" icon={<Clock className="w-4 h-4 text-purple-400" />}>
                  <div className="max-h-[120px] overflow-y-auto pr-2 space-y-3 custom-scrollbar">
                    <div className="text-xs border-l-2 border-purple-500 pl-3 py-1 bg-purple-500/5">
                      <div className="font-semibold text-slate-200">Trailing Stop Adjustment</div>
                      <div className="text-slate-500 italic">Implemented 14 days ago. ROI improved 0.4%.</div>
                    </div>
                    <div className="text-xs border-l-2 border-slate-600 pl-3 py-1 bg-slate-800/20">
                      <div className="font-semibold text-slate-400">Previous: Fixed TP Ratio</div>
                      <div className="text-slate-600 italic">Deprecated Q3.</div>
                    </div>
                  </div>
                </Section>
              </div>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              <Section title="Weaknesses & Failure Conditions" icon={<AlertTriangle className="w-4 h-4 text-red-400" />}>
                <div className="bg-red-950/10 border border-red-900/30 p-4 rounded-lg max-h-[140px] overflow-y-auto custom-scrollbar">
                  <p className="text-sm text-red-200 mb-2 font-bold">Low Liquidity Environments</p>
                  <p className="text-xs text-red-300/80 leading-relaxed">Strategy fails significantly during bank holidays and pre-FOMC consolidation. AI recommends disabling auto-execution 4 hours prior to red-folder news events.</p>
                </div>
              </Section>
              
              <Section title="Psychology Impact" icon={<Brain className="w-4 h-4 text-pink-400" />}>
                <div className="flex gap-4 h-full items-center">
                  <div className="flex-1 text-center p-3 bg-slate-900/80 rounded-lg border border-slate-800">
                    <div className="text-2xl font-bold text-pink-400 font-mono">7.8</div>
                    <div className="text-[10px] uppercase text-slate-500 mt-1 font-bold">Stress Score</div>
                  </div>
                  <div className="flex-1 text-center p-3 bg-slate-900/80 rounded-lg border border-slate-800">
                    <div className="text-2xl font-bold text-blue-400 font-mono">92%</div>
                    <div className="text-[10px] uppercase text-slate-500 mt-1 font-bold">Rule Adherence</div>
                  </div>
                </div>
              </Section>
            </div>
          </div>
        )}

        {/* Level 2: Evidence & Proof */}
        {activeLevel === 2 && (
          <div className="space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
            <h2 className="text-2xl font-bold flex items-center gap-3">
              <Database className="text-emerald-500 shrink-0" /> LEVEL 2 — EVIDENCE & PROOF
            </h2>
            
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
              <Section title="Audit Scope & Confidence">
                <div className="space-y-4">
                  <div className="flex justify-between text-sm">
                    <span className="text-slate-400 font-medium">Dataset (Total Audited)</span>
                    <span className="font-mono font-bold">1,248 Trades</span>
                  </div>
                  <div className="flex justify-between text-sm">
                    <span className="text-slate-400 font-medium">Statistical Significance</span>
                    <span className="text-emerald-400 font-bold">98.2%</span>
                  </div>
                  <div className="h-2 bg-slate-800 rounded-full">
                    <div className="h-full bg-emerald-500 rounded-full w-[98%] shadow-[0_0_15px_rgba(16,185,129,0.3)]"></div>
                  </div>
                </div>
              </Section>

              <Section title="Edge Evidence (Monte Carlo)">
                <div className="flex items-end gap-1 h-24 overflow-hidden">
                  {[40, 70, 45, 90, 65, 80, 50, 95, 100, 75, 85, 60, 40, 55, 70, 30].map((h, i) => (
                    <div key={i} className="flex-1 bg-blue-500/20 hover:bg-blue-500/50 transition-all rounded-t-sm" style={{ height: `${h}%` }}></div>
                  ))}
                </div>
                <div className="mt-4 text-xs text-slate-500 text-center uppercase tracking-widest font-bold">Simulated Expectancy Curve - N=10,000</div>
              </Section>
            </div>

            {/* NEW: Dynamic Heatmap Section */}
            <Section title="Winning vs Losing Profiles (AI Classification)">
              <div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
                {/* Alpha Profile Heatmap */}
                <div className="space-y-3">
                  <div className="text-xs font-bold text-emerald-400 uppercase tracking-wider bg-emerald-400/5 px-3 py-2 rounded-lg sticky top-0 backdrop-blur-sm z-10 border border-emerald-500/10 flex items-center justify-between">
                    <span>Alpha Profile (Success Factors)</span>
                    <span className="text-[9px] text-emerald-300/60 font-mono">{tradeData.instruments.length} Instruments</span>
                  </div>
                  
                  <div className="overflow-x-auto overflow-y-visible custom-scrollbar pb-2">
                    <HeatmapGrid 
                      instruments={tradeData.instruments}
                      factors={tradeData.winFactors}
                      correlations={tradeData.winCorrelations}
                      type="win"
                    />
                  </div>
                </div>

                {/* Failure Profile Heatmap */}
                <div className="space-y-3">
                  <div className="text-xs font-bold text-red-400 uppercase tracking-wider bg-red-400/5 px-3 py-2 rounded-lg sticky top-0 backdrop-blur-sm z-10 border border-red-500/10 flex items-center justify-between">
                    <span>Failure Profile (Decay Factors)</span>
                    <span className="text-[9px] text-red-300/60 font-mono">{tradeData.instruments.length} Instruments</span>
                  </div>
                  
                  <div className="overflow-x-auto overflow-y-visible custom-scrollbar pb-2">
                    <HeatmapGrid 
                      instruments={tradeData.instruments}
                      factors={tradeData.lossFactors}
                      correlations={tradeData.lossCorrelations}
                      type="loss"
                    />
                  </div>
                </div>
              </div>

              {/* Legend */}
              <div className="mt-6 pt-6 border-t border-slate-800/50">
                <div className="flex flex-col sm:flex-row items-center justify-between gap-4">
                  <div className="text-xs text-slate-500 uppercase tracking-wider font-bold">Correlation Intensity</div>
                  <div className="flex items-center gap-2">
                    <span className="text-[10px] text-slate-500 font-mono">Low</span>
                    <div className="flex gap-1">
                      {[20, 40, 60, 80, 100].map((intensity) => (
                        <div 
                          key={intensity}
                          className="w-8 h-4 rounded"
                          style={{
                            background: `rgba(16, 185, 129, ${intensity / 100})`,
                          }}
                        />
                      ))}
                    </div>
                    <span className="text-[10px] text-slate-500 font-mono">High</span>
                  </div>
                  <div className="text-[9px] text-slate-600 italic">← Scroll horizontally for more factors →</div>
                </div>
              </div>
            </Section>
          </div>
        )}

        {/* Level 3: Diagnostics */}
        {activeLevel === 3 && (
          <div className="space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
             <h2 className="text-2xl font-bold flex items-center gap-3">
              <Activity className="text-purple-500 shrink-0" /> LEVEL 3 — DIAGNOSTICS
            </h2>
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 items-start">
              <div className="lg:col-span-2">
                <Section title="Component Breakdown">
                  <div className="overflow-x-auto custom-scrollbar">
                    <div className="min-w-[500px]">
                      <table className="w-full text-sm text-left table-fixed">
                        <thead>
                          <tr className="text-slate-500 border-b border-slate-800">
                            <th className="pb-3 font-bold uppercase text-[10px] tracking-widest w-[40%]">Sub-Strategy</th>
                            <th className="pb-3 font-bold uppercase text-[10px] tracking-widest text-center w-[20%]">Audit Freq</th>
                            <th className="pb-3 font-bold uppercase text-[10px] tracking-widest text-center w-[20%]">Expectancy</th>
                            <th className="pb-3 font-bold uppercase text-[10px] tracking-widest text-right w-[20%]">Leakage</th>
                          </tr>
                        </thead>
                      </table>
                      <div className="max-h-[280px] overflow-y-auto custom-scrollbar">
                        <table className="w-full text-sm text-left table-fixed">
                          <tbody className="divide-y divide-slate-800/50">
                            <TableRow name="Mean Rev H1" freq="42%" exp="+1.2" dd="Low" />
                            <TableRow name="Trend Cont M15" freq="38%" exp="+0.8" dd="Med" />
                            <TableRow name="Breakout D1" freq="20%" exp="+2.4" dd="Minimal" />
                            <TableRow name="Scalp M1" freq="12%" exp="-0.2" dd="High" />
                            <TableRow name="Swing W1" freq="5%" exp="+3.1" dd="Very Low" />
                          </tbody>
                        </table>
                      </div>
                    </div>
                  </div>
                </Section>
              </div>
              <Section title="Failure Mode Analysis">
                <div className="space-y-3 max-h-[300px] overflow-y-auto pr-2 custom-scrollbar">
                  <FailureMode label="Execution Slippage" impact="High" val="0.8% ROI Decay" />
                  <FailureMode label="Pattern Overfit" impact="Med" val="Moderate Signal Noise" />
                  <FailureMode label="API Latency" impact="Low" val="Minimal Impact" />
                  <FailureMode label="Stale Data" impact="High" val="System Critical" />
                </div>
              </Section>
            </div>
            <Section title="Neuro-Diagnostic Analysis">
               <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
                  <div className="p-4 bg-slate-900/50 rounded-xl border border-slate-800 border-t-orange-500/30">
                    <div className="text-slate-500 text-xs mb-1 uppercase font-bold truncate">Decision Fatigue</div>
                    <div className="text-xl font-bold text-orange-400 truncate">Degradation after N=4</div>
                  </div>
                  <div className="p-4 bg-slate-900/50 rounded-xl border border-slate-800 border-t-emerald-500/30">
                    <div className="text-slate-500 text-xs mb-1 uppercase font-bold truncate">Emotional Drift</div>
                    <div className="text-xl font-bold text-emerald-400 truncate">Nominal (0.12 index)</div>
                  </div>
                  <div className="p-4 bg-slate-900/50 rounded-xl border border-slate-800 border-t-blue-500/30">
                    <div className="text-slate-500 text-xs mb-1 uppercase font-bold truncate">Hold-Time Fidelity</div>
                    <div className="text-xl font-bold text-blue-400 truncate">94.2% Stability</div>
                  </div>
               </div>
            </Section>
          </div>
        )}

        {/* Level 4: Action & Iteration */}
        {activeLevel === 4 && (
          <div className="space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
            <h2 className="text-2xl font-bold flex items-center gap-3">
              <PlusCircle className="text-blue-500 shrink-0" /> LEVEL 4 — ACTION & ITERATION
            </h2>

            <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 items-start">
              <Section title="AI Policy Suggestions" icon={<Zap className="w-4 h-4 text-yellow-500" />}>
                <div className="space-y-4 max-h-[350px] overflow-y-auto pr-2 custom-scrollbar">
                  {[1, 2].map((i) => (
                    <div key={i} className="p-4 bg-blue-500/5 border border-blue-500/20 rounded-xl relative overflow-hidden group">
                      <div className="absolute top-0 right-0 p-2 opacity-10 group-hover:opacity-100 transition-opacity">
                        <Cpu className="w-12 h-12 text-blue-500" />
                      </div>
                      <div className="text-blue-400 font-bold mb-1 flex items-center gap-2">
                        <PlusCircle className="w-4 h-4" /> {i === 1 ? 'Scale-In Protocol 2.0' : 'Volatility Ceiling Mod'}
                      </div>
                      <p className="text-sm text-slate-300 leading-relaxed max-h-[60px] overflow-hidden text-ellipsis line-clamp-2">
                        {i === 1 
                          ? 'System Audit suggests adding 50% size after 1:1 RR is hit. Projected net profit increase of +14.2%.' 
                          : 'Reduce aggressive position sizing by 30% when VIX exceeds 35 for 3 consecutive hours.'}
                      </p>
                      <button className="mt-4 text-[10px] font-bold text-blue-400 hover:text-blue-300 flex items-center gap-1 uppercase tracking-widest border border-blue-500/20 px-3 py-1.5 rounded-lg bg-blue-500/5 transition-all">
                        Integrate <ChevronDown className="w-3 h-3" />
                      </button>
                    </div>
                  ))}
                </div>
              </Section>

              <Section title="Audit-Enforced Guardrails" icon={<ShieldCheck className="w-4 h-4 text-emerald-500" />}>
                <div className="space-y-3 max-h-[350px] overflow-y-auto pr-2 custom-scrollbar">
                  <GuardrailItem label="Max Daily Loss" value="2.0%" status="Active" />
                  <GuardrailItem label="Correlation Limit" value="0.75" status="Active" />
                  <GuardrailItem label="Volatility Ceiling" value="VIX 35" status="Review" color="text-yellow-400" />
                  <GuardrailItem label="Max Open Pairs" value="4" status="Active" />
                  <GuardrailItem label="Margin Buffer" value="25%" status="Active" />
                </div>
              </Section>
            </div>

            <Section title="Audit Monitoring (Next Cycle Metrics)">
              <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
                {['MFE Deviation', 'Tick-Level Alpha', 'Strategy Skew', 'Kurtosis Factor'].map((metric) => (
                  <div key={metric} className="p-4 border border-slate-800 rounded-xl flex items-center justify-between bg-slate-900/20 overflow-hidden">
                    <span className="text-[10px] text-slate-400 font-bold uppercase tracking-tighter truncate pr-2">{metric}</span>
                    <div className="w-2 h-2 shrink-0 rounded-full bg-blue-500 shadow-[0_0_8px_rgba(59,130,246,0.5)] animate-pulse" />
                  </div>
                ))}
              </div>
            </Section>
          </div>
        )}
      </main>


      <style dangerouslySetInnerHTML={{ __html: `
        .custom-scrollbar::-webkit-scrollbar {
          width: 4px;
          height: 4px;
        }
        .custom-scrollbar::-webkit-scrollbar-track {
          background: transparent;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb {
          background: #334155;
          border-radius: 10px;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
          background: #475569;
        }
      `}} />
    </div>
  );
};

/* Components */

const Section = ({ title, children, icon }) => (
  <section className="bg-slate-900/40 border border-slate-800/60 rounded-2xl p-5 md:p-6 hover:border-slate-700/80 transition-all duration-300 flex flex-col h-full overflow-hidden">
    <div className="flex items-center gap-3 mb-5 shrink-0">
      {icon && <div className="p-2 bg-slate-800/50 border border-slate-700/50 rounded-lg shrink-0">{icon}</div>}
      <h3 className="text-sm font-bold uppercase tracking-[0.15em] text-slate-400 truncate">{title}</h3>
    </div>
    <div className="flex-1 overflow-hidden">
      {children}
    </div>
  </section>
);

const StatCard = ({ label, value, trend, color }) => (
  <div className="bg-slate-900/60 border border-slate-800 p-4 sm:p-5 rounded-2xl flex flex-col gap-1 hover:bg-slate-900/80 transition-all overflow-hidden">
    <span className="text-[9px] sm:text-[10px] font-bold text-slate-500 uppercase tracking-widest truncate">{label}</span>
    <div className="flex items-baseline justify-between gap-2 overflow-hidden">
      <span className={`text-xl sm:text-2xl font-bold font-mono truncate ${color}`}>{value}</span>
      <span className={`text-[9px] font-bold px-1.5 py-0.5 rounded bg-slate-800/50 shrink-0 ${trend.startsWith('+') ? 'text-emerald-400' : 'text-slate-400'}`}>
        {trend}
      </span>
    </div>
  </div>
);

const MonitorItem = ({ label, status, color }) => (
  <div className={`p-3 border-l-4 rounded-r-lg bg-slate-800/20 flex items-center justify-between border-slate-800 gap-4 ${color}`}>
    <span className="text-sm font-bold text-slate-300 tracking-tight truncate">{label}</span>
    <span className="text-[9px] uppercase font-black text-slate-500 bg-slate-800/40 px-2 py-0.5 rounded shrink-0">{status}</span>
  </div>
);

// NEW: Heatmap Grid Component
const HeatmapGrid = ({ instruments, factors, correlations, type }) => {
  const [hoveredCell, setHoveredCell] = useState(null);
  
  const getHeatColor = (value, type) => {
    const baseColor = type === 'win' ? '16, 185, 129' : '239, 68, 68'; // emerald or red
    const opacity = value / 100;
    return `rgba(${baseColor}, ${opacity})`;
  };

  const getIntensityLabel = (value) => {
    if (value >= 85) return 'Critical';
    if (value >= 70) return 'High';
    if (value >= 55) return 'Med';
    return 'Low';
  };

  return (
    <div className="min-w-max">
      {/* Header Row - Factors */}
      <div className="grid gap-1 mb-1" style={{ gridTemplateColumns: `110px repeat(${factors.length}, 80px)` }}>
        <div className="text-[9px] font-bold text-slate-600 uppercase tracking-wider p-2"></div>
        {factors.map((factor, idx) => (
          <div 
            key={idx}
            className="text-[9px] font-bold text-slate-400 uppercase tracking-tighter p-2 text-center bg-slate-800/20 rounded whitespace-nowrap overflow-hidden text-ellipsis"
            title={factor}
          >
            {factor}
          </div>
        ))}
      </div>

      {/* Data Rows */}
      <div className="space-y-1">
        {instruments.map((instrument, rowIdx) => (
          <div 
            key={instrument}
            className="grid gap-1"
            style={{ gridTemplateColumns: `110px repeat(${factors.length}, 80px)` }}
          >
            {/* Instrument Label */}
            <div className="text-xs font-bold text-slate-300 p-2 bg-slate-800/30 rounded flex items-center justify-between">
              <span>{instrument}</span>
            </div>

            {/* Heat Cells */}
            {correlations[instrument].map((value, colIdx) => (
              <div
                key={colIdx}
                className="relative p-2 rounded transition-all duration-200 cursor-pointer border border-slate-800/50 hover:border-slate-600 group"
                style={{ backgroundColor: getHeatColor(value, type) }}
                onMouseEnter={() => setHoveredCell({ row: rowIdx, col: colIdx, value, instrument, factor: factors[colIdx] })}
                onMouseLeave={() => setHoveredCell(null)}
              >
                <div className="text-[10px] font-bold font-mono text-white text-center drop-shadow-[0_1px_2px_rgba(0,0,0,0.8)]">
                  {value}%
                </div>
                
                {/* Tooltip on Hover */}
                {hoveredCell?.row === rowIdx && hoveredCell?.col === colIdx && (
                  <div className="absolute z-50 bottom-full left-1/2 -translate-x-1/2 mb-2 px-3 py-2 bg-slate-900 border border-slate-700 rounded-lg shadow-xl whitespace-nowrap">
                    <div className="text-[10px] font-bold text-slate-300">{instrument}</div>
                    <div className="text-[9px] text-slate-500">{factors[colIdx]}</div>
                    <div className="text-xs font-bold text-white mt-1">{value}% — {getIntensityLabel(value)}</div>
                    <div className="absolute top-full left-1/2 -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-l-transparent border-r-transparent border-t-slate-900"></div>
                  </div>
                )}
              </div>
            ))}
          </div>
        ))}
      </div>
    </div>
  );
};

const TableRow = ({ name, freq, exp, dd }) => (
  <tr className="group hover:bg-slate-800/20 transition-colors border-b border-slate-800/30 last:border-0">
    <td className="py-4 font-bold text-slate-300 text-xs truncate w-[40%] pr-4">{name}</td>
    <td className="py-4 text-slate-400 text-center text-xs font-mono w-[20%]">{freq}</td>
    <td className={`py-4 font-bold text-center text-xs font-mono w-[20%] ${exp.startsWith('+') ? 'text-emerald-400' : 'text-red-400'}`}>{exp}</td>
    <td className="py-4 text-slate-500 text-right text-[10px] uppercase font-bold tracking-tighter w-[20%] truncate pl-4">{dd}</td>
  </tr>
);

const FailureMode = ({ label, impact, val }) => (
  <div className="flex items-center justify-between p-3 border border-slate-800/60 rounded-xl bg-slate-900/30 gap-4">
    <div className="min-w-0">
      <div className="text-xs font-bold text-slate-200 truncate">{label}</div>
      <div className="text-[10px] text-slate-500 uppercase font-bold tracking-tighter truncate">{val}</div>
    </div>
    <span className={`text-[9px] font-black px-2 py-1 rounded tracking-widest shrink-0 ${impact === 'High' ? 'bg-red-950/30 text-red-500 border border-red-900/30' : 'bg-orange-950/30 text-orange-500 border border-orange-900/30'}`}>
      {impact}
    </span>
  </div>
);

const GuardrailItem = ({ label, value, status, color = "text-emerald-400" }) => (
  <div className="flex items-center justify-between p-3 bg-slate-800/20 rounded-xl border border-slate-700/30 gap-4">
    <div className="flex flex-col min-w-0">
      <span className="text-[9px] font-bold text-slate-500 uppercase tracking-widest truncate">{label}</span>
      <span className="text-sm font-bold font-mono truncate">{value}</span>
    </div>
    <span className={`text-[9px] font-black uppercase tracking-widest bg-slate-800/40 px-2 py-0.5 rounded shrink-0 ${color}`}>{status}</span>
  </div>
);

export default App;
